# Regras V2 Marco/2026 — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar novas regras de scoring, metas, premios e constraints de fabricante para todas as categorias a partir de marco/2026, mantendo regras V1 para meses anteriores.

**Architecture:** Modulo centralizado de regras versionadas (`rulesConfig.ts`) que determina qual versao aplicar baseado no filtro de data. Novas tabelas no Supabase para mapeamento de fabricantes de tratamentos capilares. Hooks de scoring consomem as regras do modulo central e aplicam a logica correta por versao.

**Tech Stack:** React 18, TypeScript, Vite, Supabase (PostgreSQL), React Query, Tailwind CSS, shadcn/ui

**Spec:** `docs/superpowers/specs/2026-03-13-regras-v2-marco2026-design.md`

**Nota:** Este projeto nao possui framework de testes. Verificacao via `npm run build` (compilacao) e `npm run dev` (verificacao visual no browser).

**IMPORTANTE:** As Tasks 4-7 (Chunk 2) alteram assinaturas de hooks que sao consumidos em `useDashboardData`. O build ficara quebrado ate a Task 8 (Chunk 3) alinhar as chamadas. Os commits intermediarios sao WIP — o build so volta a compilar apos Task 8.

---

## File Map

### New Files
- `src/lib/rulesConfig.ts` — Modulo centralizado de regras versionadas (V1/V2) com tipos e funcao getRulesForDate
- `src/hooks/useManufacturerData.ts` — Hook para buscar dados de fabricante (tratamento_fabricante + profissional_fabricante)

### Modified Files
- `src/hooks/useHairTreatmentData.ts` — Adicionar validacao de fabricante V2, pontos de tratamento configuraveis, substituir STAR_POINTS_VALUE por rules.starPointValue
- `src/hooks/useManicurePedicureData.ts` — Pontos de SPA configuraveis via rulesConfig, substituir STAR_POINTS_VALUE por rules.starPointValue
- `src/hooks/useEsteticaData.ts` — Modelo revenue-points V2, remover ESTETICA_REVENUE_MINIMUM export
- `src/hooks/useMaquiagemData.ts` — Modelo revenue-points V2 (era 1pt/servico)
- `src/hooks/useDashboardData.ts` — Orquestrar rulesConfig, manufacturerData, profLookup
- `src/hooks/useProfessionalDetails.ts` — Espelhar mudancas de scoring + receber starCount
- `src/components/RegrasDoDesafio.tsx` — Regras dinamicas por versao
- `src/components/PremiacaoPanel.tsx` — Metas e premios dinamicos, barras de progresso duplas
- `src/components/ProfessionalRanking.tsx` — Badges atualizados, alerta de tratamentos invalidos
- `src/components/dashboard/DataRankings.tsx` — Passar rules para ProfessionalRanking e ProfessionalModal
- `src/components/ProfessionalModal.tsx` — Breakdown V2 (validos vs invalidos, pontos faturamento + estrelas)
- `src/components/charts/EvolutionChartContainer.tsx` — Acumulacao revenue-points para Estetica/Maquiagem V2

### Database (via Supabase MCP)
- Criar tabela `tratamento_fabricante` com seed data (59 tratamentos)
- Criar tabela `profissional_fabricante` com seed data (9 registros)
- RLS policies publicas para ambas

---

## Chunk 1: Foundation

### Task 1: Criar tabelas no Supabase

**Objetivo:** Criar as tabelas `tratamento_fabricante` e `profissional_fabricante` no Supabase com dados iniciais.

- [ ] **Step 1: Criar tabela tratamento_fabricante**

Executar via `mcp__supabase__apply_migration` no projeto `kxgrprxyqeuffhczaznl`:

```sql
CREATE TABLE IF NOT EXISTS tratamento_fabricante (
  id SERIAL PRIMARY KEY,
  service_name TEXT NOT NULL,
  fabricante TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(service_name, fabricante)
);

ALTER TABLE tratamento_fabricante ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow public read access" ON tratamento_fabricante FOR SELECT USING (true);
```

- [ ] **Step 2: Criar tabela profissional_fabricante**

Executar via `mcp__supabase__apply_migration`:

```sql
CREATE TABLE IF NOT EXISTS profissional_fabricante (
  id SERIAL PRIMARY KEY,
  profissional_id INTEGER NOT NULL,
  nome_profissional TEXT NOT NULL,
  fabricante TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(profissional_id, fabricante)
);

ALTER TABLE profissional_fabricante ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow public read access" ON profissional_fabricante FOR SELECT USING (true);
```

- [ ] **Step 3: Popular profissional_fabricante**

Executar via `mcp__supabase__execute_sql`:

```sql
INSERT INTO profissional_fabricante (profissional_id, nome_profissional, fabricante) VALUES
  (788360, 'Jóia', 'L''Oréal'),
  (829346, 'Manu', 'Nuance'),
  (829346, 'Manu', 'Keune'),
  (853281, 'Sarah', 'L''Oréal'),
  (793041, 'Rilley', 'Wella'),
  (649472, 'Brenda', 'Truss'),
  (649472, 'Brenda', 'Davines'),
  (804415, 'Ricardo', 'Vitaliss'),
  (804415, 'Ricardo', 'Cadiveu')
ON CONFLICT (profissional_id, fabricante) DO NOTHING;
```

- [ ] **Step 4: Popular tratamento_fabricante**

Executar via `mcp__supabase__execute_sql`. Inserir todos os 59 tratamentos com seus fabricantes. Para tratamentos ambiguos usar `'Não classificado'`. Para "Lavagem Especial" inserir duas linhas (L'Oreal + Wella):

```sql
INSERT INTO tratamento_fabricante (service_name, fabricante) VALUES
  -- L'Oréal (12 tratamentos)
  ('Loreal Cachos', 'L''Oréal'),
  ('Loreal Inforcer', 'L''Oréal'),
  ('Loreal Metal Detox', 'L''Oréal'),
  ('Loreal Molecular', 'L''Oréal'),
  ('Loreal Nutrifier', 'L''Oréal'),
  ('Loreal Pós Quimica', 'L''Oréal'),
  ('Loreal Pro Longer', 'L''Oréal'),
  ('Loreal Scalp Argila', 'L''Oréal'),
  ('Loreal Vitamino Color Spectrum', 'L''Oréal'),
  ('Loreal Vitamino Color Tradicional', 'L''Oréal'),
  ('Kerastase Nutrição', 'L''Oréal'),
  ('Kerastase Recostrução', 'L''Oréal'),
  -- Wella (11 tratamentos, incluindo Sebastian)
  ('Wella BlondorPlex', 'Wella'),
  ('Wella Collor Brilliance', 'Wella'),
  ('Wella Color Motion', 'Wella'),
  ('Wella Ellements', 'Wella'),
  ('Wella Enrich', 'Wella'),
  ('Wella Fusion', 'Wella'),
  ('Wella Luxe Ultimate', 'Wella'),
  ('Wella Oil Reflections', 'Wella'),
  ('Wella Ultimate - reconstrução imediata', 'Wella'),
  ('Sebastian Penetraite', 'Wella'),
  ('NO BREAKER SEBASTIAN', 'Wella'),
  -- Truss (4)
  ('Truss Blond', 'Truss'),
  ('Truss Infusion', 'Truss'),
  ('Truss Net Mask', 'Truss'),
  ('Truss Nutri Infusion', 'Truss'),
  -- Davines (2)
  ('Davines calming', 'Davines'),
  ('Davines Purifying', 'Davines'),
  -- Cadiveu (3)
  ('Cadiveu Blond Reconstrutor', 'Cadiveu'),
  ('Cadiveu Hair Remedy', 'Cadiveu'),
  ('Cadiveu Nutri Glow', 'Cadiveu'),
  -- Keune (3)
  ('Keune Smoth - Reconstrução', 'Keune'),
  ('Keune Vital Hidratação', 'Keune'),
  ('Keune Vital Nutrição', 'Keune'),
  -- Nuance (1)
  ('Nuance Nutrição', 'Nuance'),
  -- Vitaliss (3)
  ('Vitallis', 'Vitaliss'),
  ('Vitallis Reposição de Carbono', 'Vitaliss'),
  ('Plastica Premium - Vitallis', 'Vitaliss'),
  -- Joico (8)
  ('Joico Blonde Life', 'Joico'),
  ('Joico Defy Damage', 'Joico'),
  ('Joico K-Pack Reconstrutor', 'Joico'),
  ('Joico k-pak 4 passos', 'Joico'),
  ('Joico K-Pak Color Therapy', 'Joico'),
  ('Joico K-PAK Revitaluxe', 'Joico'),
  ('Joico Moisture Recovery', 'Joico'),
  ('Trat. Joico Luster lock', 'Joico'),
  -- Lavagem Especial (multi-fabricante: L'Oréal + Wella)
  ('Lavagem Especial (loreal, wella,kerastase, joico)', 'L''Oréal'),
  ('Lavagem Especial (loreal, wella,kerastase, joico)', 'Wella'),
  -- Não classificados (11)
  ('Banho de Ouro', 'Não classificado'),
  ('Cauterização Trivitt', 'Não classificado'),
  ('Detox Capilar e escova', 'Não classificado'),
  ('Donatti Profissional', 'Não classificado'),
  ('Hidratação Princesse', 'Não classificado'),
  ('Olaplex Donatti', 'Não classificado'),
  ('Plastica dos Fios', 'Não classificado'),
  ('Schwarzkopf bonacure', 'Não classificado'),
  ('Senscience Blond', 'Não classificado'),
  ('Senscience Nutrição', 'Não classificado'),
  ('Trat. CPR', 'Não classificado')
ON CONFLICT (service_name, fabricante) DO NOTHING;
```

- [ ] **Step 5: Verificar dados inseridos**

Executar via `mcp__supabase__execute_sql`:

```sql
SELECT COUNT(*) as total FROM tratamento_fabricante;
-- Esperado: 61 (59 tratamentos + 2 extra para Lavagem Especial multi-fabricante)

SELECT COUNT(*) as total FROM profissional_fabricante;
-- Esperado: 9
```

- [ ] **Step 6: Salvar migration local**

Criar arquivo `supabase/migrations/20260313_create_manufacturer_tables.sql` com o SQL dos steps 1-4 combinados para documentacao.

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/20260313_create_manufacturer_tables.sql
git commit -m "feat(db): criar tabelas tratamento_fabricante e profissional_fabricante"
```

---

### Task 2: Criar modulo rulesConfig.ts

**Files:**
- Create: `src/lib/rulesConfig.ts`

- [ ] **Step 1: Criar o modulo com tipos e regras V1/V2**

Criar `src/lib/rulesConfig.ts`:

```typescript
export interface CategoryRules {
  scoringModel: 'points' | 'revenue-percentage' | 'revenue-points';
  clientPointValue: number;
  specialServicePointValue: number;
  specialServiceLabel: string;
  starPointValue: number;
  starsCountInScore: boolean;
  revenuePointConversion?: number;
  qualificationGoals: {
    minUniqueClients?: number;
    minSpecialServices?: number;
    minRevenue?: number;
    minServices?: number;
  };
  symbolicGoals: {
    stars?: number;
  };
  manufacturerConstraints: boolean;
  prize: string;
}

export interface RulesVersion {
  id: string;
  validFrom: string;
  cabelo: CategoryRules;
  unhas: CategoryRules;
  estetica: CategoryRules;
  maquiagem: CategoryRules;
}

const RULES_V1: RulesVersion = {
  id: 'v1',
  validFrom: '2000-01',
  cabelo: {
    scoringModel: 'points',
    clientPointValue: 1,
    specialServicePointValue: 2,
    specialServiceLabel: 'Tratamentos',
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 60 },
    symbolicGoals: {},
    manufacturerConstraints: false,
    prize: 'R$300',
  },
  unhas: {
    scoringModel: 'points',
    clientPointValue: 1,
    specialServicePointValue: 2,
    specialServiceLabel: 'SPA dos Pés',
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 50 },
    symbolicGoals: {},
    manufacturerConstraints: false,
    prize: 'R$200',
  },
  estetica: {
    scoringModel: 'revenue-percentage',
    clientPointValue: 0,
    specialServicePointValue: 0,
    specialServiceLabel: 'Serviços',
    starPointValue: 3,
    starsCountInScore: false,
    revenuePointConversion: undefined,
    qualificationGoals: { minRevenue: 5000 },
    symbolicGoals: {},
    manufacturerConstraints: false,
    prize: 'R$200',
  },
  maquiagem: {
    scoringModel: 'points',
    clientPointValue: 0,
    specialServicePointValue: 1,
    specialServiceLabel: 'Serviços',
    starPointValue: 3,
    starsCountInScore: false,
    qualificationGoals: { minServices: 25 },
    symbolicGoals: {},
    manufacturerConstraints: false,
    prize: 'R$200',
  },
};

const RULES_V2: RulesVersion = {
  id: 'v2',
  validFrom: '2026-03',
  cabelo: {
    scoringModel: 'points',
    clientPointValue: 1,
    specialServicePointValue: 3,
    specialServiceLabel: 'Tratamentos',
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 50, minSpecialServices: 40 },
    symbolicGoals: { stars: 30 },
    manufacturerConstraints: true,
    prize: 'SPA Especial + Voucher surpresa',
  },
  unhas: {
    scoringModel: 'points',
    clientPointValue: 1,
    specialServicePointValue: 3,
    specialServiceLabel: 'SPA dos Pés',
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 70, minSpecialServices: 10 },
    symbolicGoals: { stars: 30 },
    manufacturerConstraints: false,
    prize: 'SPA Especial + Voucher surpresa',
  },
  estetica: {
    scoringModel: 'revenue-points',
    clientPointValue: 0,
    specialServicePointValue: 0,
    specialServiceLabel: 'Serviços',
    starPointValue: 3,
    starsCountInScore: true,
    revenuePointConversion: 100,
    qualificationGoals: { minRevenue: 10000 },
    symbolicGoals: { stars: 30 },
    manufacturerConstraints: false,
    prize: 'SPA Especial + Voucher surpresa',
  },
  maquiagem: {
    scoringModel: 'revenue-points',
    clientPointValue: 0,
    specialServicePointValue: 0,
    specialServiceLabel: 'Serviços',
    starPointValue: 3,
    starsCountInScore: true,
    revenuePointConversion: 100,
    qualificationGoals: { minRevenue: 3500 },
    symbolicGoals: { stars: 15 },
    manufacturerConstraints: false,
    prize: 'SPA Especial + Voucher surpresa',
  },
};

const RULES_VERSIONS: RulesVersion[] = [RULES_V1, RULES_V2];

export function getRulesForDate(startDate: string): RulesVersion {
  const yearMonth = startDate.substring(0, 7); // 'YYYY-MM'
  let selectedVersion = RULES_VERSIONS[0];
  for (const version of RULES_VERSIONS) {
    if (yearMonth >= version.validFrom) {
      selectedVersion = version;
    }
  }
  return selectedVersion;
}

export function getCategoryRules(
  rules: RulesVersion,
  category: string
): CategoryRules {
  const key = category.toLowerCase() as keyof Omit<RulesVersion, 'id' | 'validFrom'>;
  return rules[key] as CategoryRules;
}
```

- [ ] **Step 2: Verificar compilacao**

Run: `npm run build`
Expected: Build sem erros (modulo novo, sem consumidores ainda)

- [ ] **Step 3: Commit**

```bash
git add src/lib/rulesConfig.ts
git commit -m "feat: criar modulo rulesConfig com regras V1/V2 versionadas"
```

---

### Task 3: Criar hook useManufacturerData

**Files:**
- Create: `src/hooks/useManufacturerData.ts`

- [ ] **Step 1: Criar o hook**

Criar `src/hooks/useManufacturerData.ts`:

```typescript
import { useQuery } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";

interface TratamentoFabricante {
  service_name: string;
  fabricante: string;
}

interface ProfissionalFabricante {
  profissional_id: number;
  nome_profissional: string;
  fabricante: string;
}

export interface ManufacturerData {
  getTreatmentManufacturers: (serviceName: string) => string[];
  getProfessionalAllowedManufacturers: (profissionalId: number) => string[];
  isTreatmentAllowed: (serviceName: string, profissionalId: number) => boolean;
  isLoading: boolean;
}

export function useManufacturerData(enabled: boolean = true): ManufacturerData {
  const { data: treatmentMap, isLoading: treatmentLoading } = useQuery({
    queryKey: ['tratamento_fabricante'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('tratamento_fabricante' as any)
        .select('service_name, fabricante');

      if (error) throw error;

      const map = new Map<string, string[]>();
      for (const row of (data as TratamentoFabricante[])) {
        const existing = map.get(row.service_name) || [];
        existing.push(row.fabricante);
        map.set(row.service_name, existing);
      }
      return map;
    },
    enabled,
    staleTime: 5 * 60 * 1000,
  });

  const { data: profMap, isLoading: profLoading } = useQuery({
    queryKey: ['profissional_fabricante'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('profissional_fabricante' as any)
        .select('profissional_id, nome_profissional, fabricante');

      if (error) throw error;

      const map = new Map<number, string[]>();
      for (const row of (data as ProfissionalFabricante[])) {
        const existing = map.get(row.profissional_id) || [];
        existing.push(row.fabricante);
        map.set(row.profissional_id, existing);
      }
      return map;
    },
    enabled,
    staleTime: 5 * 60 * 1000,
  });

  const getTreatmentManufacturers = (serviceName: string): string[] => {
    return treatmentMap?.get(serviceName) || [];
  };

  const getProfessionalAllowedManufacturers = (profissionalId: number): string[] => {
    return profMap?.get(profissionalId) || [];
  };

  const isTreatmentAllowed = (serviceName: string, profissionalId: number): boolean => {
    const treatmentManufacturers = getTreatmentManufacturers(serviceName);
    const allowedManufacturers = getProfessionalAllowedManufacturers(profissionalId);

    if (treatmentManufacturers.length === 0) return false;
    if (allowedManufacturers.length === 0) return false;

    return treatmentManufacturers.some(
      tm => tm !== 'Não classificado' && allowedManufacturers.includes(tm)
    );
  };

  return {
    getTreatmentManufacturers,
    getProfessionalAllowedManufacturers,
    isTreatmentAllowed,
    isLoading: enabled ? (treatmentLoading || profLoading) : false,
  };
}
```

- [ ] **Step 2: Verificar compilacao**

Run: `npm run build`
Expected: Build sem erros

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useManufacturerData.ts
git commit -m "feat: criar hook useManufacturerData para validacao de fabricantes"
```

---

## Chunk 2: Scoring Hooks

### Task 4: Atualizar useHairTreatmentData

**Files:**
- Modify: `src/hooks/useHairTreatmentData.ts`

**Contexto:** Este hook precisa:
1. Ler pontos de tratamento de rulesConfig (2→3 no V2)
2. Validar fabricante quando manufacturerConstraints === true
3. Retornar invalidTreatments para a UI sinalizar
4. Receber profLookup para resolver nome→profissionalId

- [ ] **Step 1: Adicionar imports e atualizar parametros da funcao**

No topo do arquivo, adicionar imports de rulesConfig e ManufacturerData. Atualizar a assinatura de `processHairTreatmentData` para receber `rules` e `manufacturerData` e `profLookup`:

```typescript
// Adicionar imports no topo:
import { CategoryRules } from "@/lib/rulesConfig";
import { ManufacturerData } from "@/hooks/useManufacturerData";
import { ProfissionalAtivo } from "@/types/profissionaisAtivos";

// Na interface de retorno do processamento, adicionar:
export interface InvalidTreatment {
  professional: string;
  serviceName: string;
  fabricante: string;
}
```

Atualizar assinatura de `processHairTreatmentData`:
- Adicionar parametro `rules: CategoryRules`
- Adicionar parametro `manufacturerData: ManufacturerData | null`
- Adicionar parametro `profLookup: Map<string, ProfissionalAtivo>`

- [ ] **Step 2: Substituir constantes hardcoded por rules**

Dentro de `processHairTreatmentData`:

1. Substituir o valor hardcoded `2` (pontos de tratamento) por `rules.specialServicePointValue`
2. Substituir `STAR_POINTS_VALUE` (importado de useStarsData) por `rules.starPointValue` em todos os calculos de pontos de estrela. Remover o import de `STAR_POINTS_VALUE`
3. Quando `rules.manufacturerConstraints` === true:
   - Para cada servico com `category === "Tratamentos para Cabelo"`:
     - Resolver profissionalId via `profLookup.get(professional)?.profissionalId`
     - Chamar `manufacturerData.isTreatmentAllowed(service_name, profissionalId)`
     - Se **permitido**: contar pontos normalmente
     - Se **nao permitido**: adicionar ao array `invalidTreatments` com o fabricante (via `manufacturerData.getTreatmentManufacturers(service_name)[0] || 'Desconhecido'`), NAO contar pontos
4. O campo `treatmentServices` no output deve contar APENAS tratamentos validos quando V2. Adicionar campo `invalidTreatmentCount` para contagem de invalidos
5. Retornar `invalidTreatments` junto com os dados existentes

- [ ] **Step 3: Atualizar o hook principal useHairTreatmentData**

O hook principal que chama `processHairTreatmentData` precisa:
- Receber `rules` e `manufacturerData` e `profLookup` como parametros
- Passar esses parametros para `processHairTreatmentData`
- Retornar `invalidTreatments` no retorno do hook

A assinatura do hook muda de:
```typescript
export function useHairTreatmentData(
  allServicesData: any[],
  categoryProfessionals: string[],
  starsByProfessional: Map<string, number>,
  dateRange: { startDate: string; endDate: string }
)
```
Para:
```typescript
export function useHairTreatmentData(
  allServicesData: any[],
  categoryProfessionals: string[],
  starsByProfessional: Map<string, number>,
  dateRange: { startDate: string; endDate: string },
  rules: CategoryRules,
  manufacturerData: ManufacturerData | null,
  profLookup: Map<string, ProfissionalAtivo>
)
```

- [ ] **Step 4: Garantir que todos os profissionais aparecem no ranking**

O codigo atual inicializa `professionalPoints` a partir dos dados de servico filtrados (`treatmentData`). Um profissional sem tratamentos (ou com todos invalidos no V2) desaparece do ranking. Refatorar para inicializar a partir de `categoryProfessionals`:

```typescript
// NO INICIO de processHairTreatmentData, ANTES de iterar sobre treatmentData:
const professionalPoints = new Map<string, { points: number; uniqueClientDays: number; treatmentServices: number; invalidTreatmentCount: number; services: any[]; starCount: number; starPoints: number }>();

// Inicializar TODOS os profissionais da categoria com 0
for (const profName of categoryProfessionals) {
  professionalPoints.set(profName, {
    points: 0, uniqueClientDays: 0, treatmentServices: 0,
    invalidTreatmentCount: 0, services: [], starCount: 0, starPoints: 0
  });
}

// DEPOIS iterar sobre treatmentData para somar pontos (codigo existente, adaptado)
```

- [ ] **Step 5: Verificar compilacao (vai falhar — esperado)**

Run: `npm run build`
Expected: Erros de compilacao em `useDashboardData.ts` porque a assinatura mudou. Isso sera corrigido na Task 8.

- [ ] **Step 6: Commit (WIP)**

```bash
git add src/hooks/useHairTreatmentData.ts
git commit -m "feat(hooks): adicionar validacao de fabricante e scoring V2 no useHairTreatmentData"
```

---

### Task 5: Atualizar useManicurePedicureData

**Files:**
- Modify: `src/hooks/useManicurePedicureData.ts`

**Contexto:** Mudanca simples — pontos de SPA de 2→3 via rulesConfig.

- [ ] **Step 1: Adicionar import e parametro de rules**

```typescript
import { CategoryRules } from "@/lib/rulesConfig";
```

Adicionar parametro `rules: CategoryRules` a assinatura do hook:

```typescript
export function useManicurePedicureData(
  allServicesData: any[],
  categoryProfessionals: string[],
  starsByProfessional: Map<string, number>,
  dateRange: { startDate: string; endDate: string },
  rules: CategoryRules
)
```

- [ ] **Step 2: Substituir constantes hardcoded por rules**

Na funcao `processManicurePedicureData`:
- Substituir o valor `2` (pontos por SPA) por `rules.specialServicePointValue`
- Substituir `STAR_POINTS_VALUE` por `rules.starPointValue` em todos os calculos de pontos de estrela. Remover o import de `STAR_POINTS_VALUE`

- [ ] **Step 3: Garantir que todos os profissionais aparecem no ranking**

Mesmo fix da Task 4 Step 4: inicializar `professionalPoints` a partir de `categoryProfessionals` antes de iterar sobre dados de servico:

```typescript
const professionalPoints = new Map<string, { points: number; uniqueClientDays: number; spaServices: number; services: any[]; starCount: number; starPoints: number }>();

for (const profName of categoryProfessionals) {
  professionalPoints.set(profName, {
    points: 0, uniqueClientDays: 0, spaServices: 0,
    services: [], starCount: 0, starPoints: 0
  });
}
```

- [ ] **Step 4: Commit (WIP)**

```bash
git add src/hooks/useManicurePedicureData.ts
git commit -m "feat(hooks): usar rulesConfig para pontos de SPA no useManicurePedicureData"
```

---

### Task 6: Atualizar useEsteticaData

**Files:**
- Modify: `src/hooks/useEsteticaData.ts`

**Contexto:** Mudanca significativa — V1 mantem revenue-percentage, V2 muda para revenue-points onde R$100=1pt + estrelas contam 3pts cada.

- [ ] **Step 1: Adicionar import e parametro de rules**

```typescript
import { CategoryRules } from "@/lib/rulesConfig";
```

Adicionar parametro `rules: CategoryRules` a assinatura do hook.

Remover o export `ESTETICA_REVENUE_MINIMUM = 5000` — consumidores passarao a usar `rules.estetica.qualificationGoals.minRevenue`.

- [ ] **Step 2: Implementar logica condicional V1/V2**

Na funcao `processEsteticaData`:

**Se `rules.scoringModel === 'revenue-percentage'` (V1):**
- Manter logica atual: `revenuePercentage = Math.round(((totalRevenue / rules.qualificationGoals.minRevenue!) * 100) * 10) / 10`
- Estrelas: display only (nao somam)
- Ranking por `revenuePercentage`

**Se `rules.scoringModel === 'revenue-points'` (V2):**
- `revenuePoints = Math.floor(totalRevenue / rules.revenuePointConversion!)`
- `starPoints = starCount * rules.starPointValue`
- `totalPoints = revenuePoints + starPoints`
- Ranking por `totalPoints`
- Output inclui: `totalRevenue`, `revenuePoints`, `starPoints`, `totalPoints`
- `revenuePercentage` calculado para barra de progresso: `Math.round(((totalRevenue / rules.qualificationGoals.minRevenue!) * 100) * 10) / 10`

O array de retorno do profissional deve incluir ambos os campos para que os componentes possam usar o correto.

- [ ] **Step 3: Atualizar assinatura do hook**

O hook principal passa a aceitar `rules`:

```typescript
export function useEsteticaData(
  allServicesData: any[],
  categoryProfessionals: string[],
  starsByProfessional: Map<string, number>,
  dateRange: { startDate: string; endDate: string },
  rules: CategoryRules
)
```

- [ ] **Step 4: Commit (WIP)**

```bash
git add src/hooks/useEsteticaData.ts
git commit -m "feat(hooks): implementar modelo revenue-points V2 no useEsteticaData"
```

---

### Task 7: Atualizar useMaquiagemData

**Files:**
- Modify: `src/hooks/useMaquiagemData.ts`

**Contexto:** V1 = 1pt por servico. V2 = revenue-points identico a Estetica (R$100=1pt + estrelas 3pts).

- [ ] **Step 1: Adicionar import e parametro de rules**

```typescript
import { CategoryRules } from "@/lib/rulesConfig";
```

Adicionar parametro `rules: CategoryRules` a assinatura do hook.

- [ ] **Step 2: Implementar logica condicional V1/V2**

Na funcao `processMaquiagemData`:

**Se `rules.scoringModel === 'points'` (V1):**
- Manter logica atual: 1 ponto por servico, sem deduplicacao
- Estrelas: display only
- Ranking por `points`

**Se `rules.scoringModel === 'revenue-points'` (V2):**
- Mesma logica de Estetica V2:
  - `revenuePoints = Math.floor(totalRevenue / rules.revenuePointConversion!)`
  - `starPoints = starCount * rules.starPointValue`
  - `totalPoints = revenuePoints + starPoints`
- Ranking por `totalPoints`
- Output inclui: `totalRevenue`, `revenuePoints`, `starPoints`, `totalPoints`
- `revenuePercentage` calculado para barra de progresso

- [ ] **Step 3: Commit (WIP)**

```bash
git add src/hooks/useMaquiagemData.ts
git commit -m "feat(hooks): implementar modelo revenue-points V2 no useMaquiagemData"
```

---

## Chunk 3: Orchestration

### Task 8: Atualizar useDashboardData

**Files:**
- Modify: `src/hooks/useDashboardData.ts`

**Contexto:** Hub central que orquestra todos os hooks. Precisa passar rulesConfig, manufacturerData e profLookup para os hooks de categoria.

- [ ] **Step 1: Adicionar imports**

```typescript
import { getRulesForDate } from "@/lib/rulesConfig";
import { useManufacturerData } from "@/hooks/useManufacturerData";
```

- [ ] **Step 2: Obter regras, dateRange completo, manufacturer data e profLookup**

Dentro do hook, antes das chamadas dos hooks de categoria:

```typescript
const dateRange = useDateFilter().getFilteredDateRange(); // objeto completo {startDate, endDate}
const rules = getRulesForDate(dateRange.startDate);
const manufacturerData = useManufacturerData(rules.cabelo.manufacturerConstraints);
```

Atualizar a desestruturacao de `useActiveProfessionals()` para incluir `profLookup`:

```typescript
// ANTES:
const { activeProfessionals, getProfessionalsByCategory, loading: profsLoading, fetchActiveProfessionals } = useActiveProfessionals();
// DEPOIS:
const { activeProfessionals, getProfessionalsByCategory, profLookup, loading: profsLoading, fetchActiveProfessionals } = useActiveProfessionals();
```

`profLookup` ja e exposto por `useActiveProfessionals` (Map<string, ProfissionalAtivo> mapeando nome → profissional).

- [ ] **Step 3: Passar parametros atualizados para hooks de categoria**

Atualizar todas as chamadas:

```typescript
const hairData = useHairTreatmentData(
  allServicesData, cabeloProfessionals, starsByCategory.cabelo, dateRange,
  rules.cabelo, manufacturerData, profLookup
);

const nailData = useManicurePedicureData(
  allServicesData, unhasProfessionals, starsByCategory.unhas, dateRange,
  rules.unhas
);

const esteticaData = useEsteticaData(
  allServicesData, esteticaProfessionals, starsByCategory.estetica, dateRange,
  rules.estetica
);

const maquiagemData = useMaquiagemData(
  allServicesData, maquiagemProfessionals, starsByCategory.maquiagem, dateRange,
  rules.maquiagem
);
```

- [ ] **Step 4: Atualizar loading state**

```typescript
const isLoading = servicesLoading || profsLoading || starsLoading ||
  (rules.cabelo.manufacturerConstraints && manufacturerData.isLoading);
```

- [ ] **Step 5: Expor rules e invalidTreatments no retorno**

Adicionar ao objeto de retorno:
- `rules` (a versao ativa)
- `invalidTreatments` (do hairData)
- `manufacturerData` (para uso no ProfessionalModal)

- [ ] **Step 6: Verificar compilacao**

Run: `npm run build`
Expected: Build deve passar agora que as assinaturas estao alinhadas

- [ ] **Step 7: Commit**

```bash
git add src/hooks/useDashboardData.ts
git commit -m "feat(hooks): orquestrar rulesConfig e manufacturerData no useDashboardData"
```

---

### Task 9: Atualizar useProfessionalDetails

**Files:**
- Modify: `src/hooks/useProfessionalDetails.ts`

**Contexto:** Este hook reimplementa toda a logica de scoring para o modal de detalhes. Deve espelhar as mudancas dos hooks de categoria.

- [ ] **Step 1: Adicionar imports e reestruturar parametros do hook**

```typescript
import { CategoryRules, getRulesForDate } from "@/lib/rulesConfig";
import { ManufacturerData } from "@/hooks/useManufacturerData";
```

**IMPORTANTE — Arquitetura:** `useProfessionalDetails` e um hook auto-contido que busca dados do Supabase diretamente via `fetchProfessionalDetails(professional, category)`. Ele NAO recebe dados pre-processados. Para passar rules/manufacturerData/starCount, ha duas opcoes:

**Opcao recomendada:** Adicionar parametros ao hook que ficam disponiveis dentro do `fetchProfessionalDetails` useCallback:

```typescript
export function useProfessionalDetails(
  rules: RulesVersion,          // versao completa das regras
  manufacturerData: ManufacturerData | null,
  profLookup: Map<string, ProfissionalAtivo>,
  starsByCategory: StarsByCategory  // para buscar starCount do profissional
)
```

Dentro de `fetchProfessionalDetails`, usar:
- `getCategoryRules(rules, category)` para obter as regras da categoria
- `manufacturerData?.isTreatmentAllowed(...)` para validar fabricante
- `profLookup.get(professional)?.profissionalId` para resolver ID
- `starsByCategory[category]?.get(professional) || 0` para obter starCount

Em `useDashboardData`, atualizar a chamada:
```typescript
const { details, selectProfessional, ... } = useProfessionalDetails(
  rules, manufacturerData, profLookup, starsByCategory
);
```

- [ ] **Step 2: Atualizar logica de scoring para Cabelo**

Na secao que processa Cabelo:
- Usar `rules.specialServicePointValue` em vez de hardcoded `2`
- Se `rules.manufacturerConstraints` e true e `manufacturerData` disponivel:
  - Validar cada tratamento via `manufacturerData.isTreatmentAllowed(service_name, profissionalId)`
  - Separar tratamentos validos (contam pontos) de invalidos (nao contam)
  - Retornar lista de tratamentos invalidos no summary

- [ ] **Step 3: Atualizar logica de scoring para Unhas**

Na secao que processa Unhas:
- Usar `rules.specialServicePointValue` em vez de hardcoded `2`

- [ ] **Step 4: Atualizar logica de scoring para Estetica**

Na secao que processa Estetica:
- **V1** (`rules.scoringModel === 'revenue-percentage'`): manter logica atual
- **V2** (`rules.scoringModel === 'revenue-points'`):
  - `revenuePoints = Math.floor(totalRevenue / rules.revenuePointConversion!)`
  - `starPoints = starCount * rules.starPointValue`
  - `totalPoints = revenuePoints + starPoints`
  - Atualizar summary com `revenuePoints`, `starPoints`, `totalPoints`

- [ ] **Step 5: Atualizar logica de scoring para Maquiagem**

Na secao que processa Maquiagem:
- **V1** (`rules.scoringModel === 'points'`): manter logica 1pt/servico
- **V2** (`rules.scoringModel === 'revenue-points'`): mesma logica de Estetica V2

- [ ] **Step 6: Remover import de ESTETICA_REVENUE_MINIMUM**

Substituir `import { ESTETICA_REVENUE_MINIMUM } from './useEsteticaData'` por uso de `rules.qualificationGoals.minRevenue`.

- [ ] **Step 7: Verificar compilacao**

Run: `npm run build`
Expected: Build sem erros

- [ ] **Step 8: Commit**

```bash
git add src/hooks/useProfessionalDetails.ts
git commit -m "feat(hooks): espelhar regras V2 no useProfessionalDetails"
```

---

## Chunk 4: UI Components

### Task 10: Atualizar RegrasDoDesafio

**Files:**
- Modify: `src/components/RegrasDoDesafio.tsx`

- [ ] **Step 1: Tornar o componente dinamico**

Adicionar imports:
```typescript
import { useDateFilter } from "@/contexts/DateFilterContext";
import { getRulesForDate, RulesVersion } from "@/lib/rulesConfig";
```

Dentro do componente, obter as regras ativas:
```typescript
const { getFilteredDateRange } = useDateFilter();
const { startDate } = getFilteredDateRange();
const rules = getRulesForDate(startDate);
```

- [ ] **Step 2: Substituir array REGRAS hardcoded por funcao geradora**

Criar funcao `buildRegras(rules: RulesVersion)` que retorna o array REGRAS baseado na versao ativa. A funcao gera os textos corretos:

**Cabelo V1:** textos atuais (2 pontos por tratamento, meta 60 clientes)
**Cabelo V2:**
```
regras: [
  "Cada cliente atendido no dia vale 1 ponto (mesmo cliente no mesmo dia conta só 1 vez)",
  "Cada tratamento capilar da marca autorizada vale 3 pontos",
  "Cada avaliação Google aprovada vale 3 pontos",
]
meta: "Metas mínimas: 50 clientes únicos + 40 tratamentos no mês"
metaExcelencia: "Meta excelência profissional: Estrelas Google: 30 estrelas"
```

**Unhas V2:**
```
regras: [
  "Cada cliente atendido no dia vale 1 ponto (mesmo cliente no mesmo dia conta só 1 vez)",
  "Cada SPA dos Pés realizado vale 3 pontos",
  "Cada avaliação Google aprovada vale 3 pontos",
]
meta: "Metas mínimas: 70 clientes únicos + 10 SPA dos Pés no mês"
metaExcelencia: "Meta excelência profissional: Estrelas Google: 30 estrelas"
```

**Estetica V2:**
```
regras: [
  "A pontuação é baseada no faturamento: a cada R$ 100,00 faturados, você ganha 1 ponto",
  "Cada avaliação Google aprovada vale 3 pontos",
  "Existe uma meta mínima de faturamento para se qualificar ao prêmio",
]
metaExcelencia: "Meta excelência profissional: Estrelas Google: 30 estrelas"
```

**Maquiagem V2:**
```
regras: [
  "A pontuação é baseada no faturamento: a cada R$ 100,00 faturados, você ganha 1 ponto",
  "Cada avaliação Google aprovada vale 3 pontos",
  "Existe uma meta mínima de faturamento para se qualificar ao prêmio",
]
metaExcelencia: "Meta excelência profissional: Estrelas Google: 15 estrelas"
```

Premio V2 (todas): `"SPA Especial + Voucher surpresa"`

- [ ] **Step 3: Adicionar campo metaExcelencia ao render**

No JSX, apos o bloco de `meta`, adicionar render condicional para `metaExcelencia` quando existir (so V2+):

```tsx
{categoria.metaExcelencia && (
  <div className={`text-xs font-medium ${categoria.colorScheme.text} bg-white/40 rounded-lg px-3 py-1.5 border ${categoria.colorScheme.border} mt-1`}>
    {categoria.metaExcelencia}
  </div>
)}
```

- [ ] **Step 4: Verificar compilacao e visual**

Run: `npm run build` — deve compilar
Run: `npm run dev` — verificar no browser que:
- Mes atual (marco/2026) mostra regras V2
- Mes anterior (fevereiro) mostra regras V1

- [ ] **Step 5: Commit**

```bash
git add src/components/RegrasDoDesafio.tsx
git commit -m "feat(ui): tornar RegrasDoDesafio dinamico com regras versionadas"
```

---

### Task 11: Atualizar PremiacaoPanel

**Files:**
- Modify: `src/components/PremiacaoPanel.tsx`

- [ ] **Step 1: Substituir PREMIACAO_CONFIG hardcoded por rulesConfig**

Remover o objeto `PREMIACAO_CONFIG` hardcoded. Importar rulesConfig:

```typescript
import { getRulesForDate, getCategoryRules, RulesVersion } from "@/lib/rulesConfig";
import { useDateFilter } from "@/contexts/DateFilterContext";
```

Dentro do componente, obter as regras:
```typescript
const { getFilteredDateRange } = useDateFilter();
const { startDate } = getFilteredDateRange();
const rules = getRulesForDate(startDate);
```

- [ ] **Step 2: Atualizar logica de qualificacao**

Para cada categoria, usar as regras da versao ativa:

**Cabelo V2:** Qualificado = `uniqueClients >= 50 AND validTreatments >= 40` (dupla verificacao)
**Unhas V2:** Qualificado = `uniqueClients >= 70 AND spaCount >= 10` (dupla verificacao)
**Estetica V2:** Qualificado = `totalRevenue >= 10000` (via revenuePercentage >= 100)
**Maquiagem V2:** Qualificado = `totalRevenue >= 3500` (via revenuePercentage >= 100)

Para categorias com dois criterios (Cabelo/Unhas V2), o profissional so e qualificado se AMBOS forem atingidos.

- [ ] **Step 3: Implementar barras de progresso duplas para V2**

Para Cabelo e Unhas V2, exibir duas barras de progresso empilhadas:
1. Barra de clientes unicos (ex: "42/50 clientes")
2. Barra de servico especial (ex: "35/40 tratamentos")

Ambas precisam estar 100% para qualificar.

Para Estetica/Maquiagem V2, manter barra unica mostrando "X% da meta" (baseado em revenuePercentage).

- [ ] **Step 4: Atualizar exibicao de premios**

Usar `rules.cabelo.prize`, `rules.unhas.prize`, etc. para exibir o premio correto por versao.

- [ ] **Step 5: Verificar compilacao e visual**

Run: `npm run build`
Run: `npm run dev` — verificar no browser

- [ ] **Step 6: Commit**

```bash
git add src/components/PremiacaoPanel.tsx
git commit -m "feat(ui): atualizar PremiacaoPanel com metas e premios dinamicos V2"
```

---

### Task 12: Atualizar ProfessionalRanking

**Files:**
- Modify: `src/components/ProfessionalRanking.tsx`

- [ ] **Step 1: Receber rules como prop**

Adicionar prop `rules` ao componente (a ser passado pelo pai DataRankings).

- [ ] **Step 2: Atualizar badges de categoria**

**Cabelo V2:**
- Badge de tratamentos mostra so tratamentos validos (da marca autorizada)
- Novo badge de alerta (icone warning) quando o profissional tem tratamentos invalidos, com tooltip mostrando quantidade

**Estetica/Maquiagem V2:**
- Trocar display de "45%" ou "12 serv" por "XX Pts" (totalPoints)
- Manter badge de estrelas quando presente

- [ ] **Step 3: Atualizar display de score**

Na logica de exibicao de pontuacao:
- V1 Estetica: mostra porcentagem (`revenuePercentage + "%"`)
- V2 Estetica/Maquiagem: mostra pontos (`totalPoints + " Pts"`)
- Demais: mostra pontos (ja funciona assim)

- [ ] **Step 4: Atualizar DataRankings para passar rules**

Modificar `src/components/dashboard/DataRankings.tsx`:
1. Receber `rules` como prop (vindo de Index.tsx ou useDashboardData)
2. Passar `rules` para cada `ProfessionalRanking`
3. Passar `rules` para `ProfessionalModal` (tambem renderizado dentro de DataRankings)

- [ ] **Step 5: Verificar compilacao e visual**

Run: `npm run build`
Run: `npm run dev`

- [ ] **Step 6: Commit**

```bash
git add src/components/ProfessionalRanking.tsx src/components/dashboard/DataRankings.tsx
git commit -m "feat(ui): atualizar badges e display de score no ProfessionalRanking para V2"
```

---

### Task 13: Atualizar ProfessionalModal

**Files:**
- Modify: `src/components/ProfessionalModal.tsx`

- [ ] **Step 1: Receber rules como prop**

Adicionar props `rules` e `rulesVersion` (para saber se e V1 ou V2).

- [ ] **Step 2: Atualizar renderSummaryCards para Cabelo V2**

Quando V2 + Cabelo:
- Card de tratamentos mostra contagem de tratamentos **validos** (marca autorizada)
- Novo card ou secao de alerta mostrando tratamentos **nao contabilizados** com o fabricante identificado

- [ ] **Step 3: Atualizar renderSummaryCards para Estetica/Maquiagem V2**

Quando V2:
- Substituir card de "Percentual da Meta" / "Servicos Realizados" por breakdown:
  - Card 1: "Pontos de Faturamento: X" (revenuePoints)
  - Card 2: "Pontos de Estrelas: Y" (starPoints)
  - Total exibido no DialogDescription: "XX Pts total"

- [ ] **Step 4: Atualizar DialogDescription**

```tsx
<DialogDescription className="mt-1">
  {displayCategory}: {details.totalServices} serviços |
  {rules.scoringModel === 'revenue-points'
    ? ` ${details.totalPoints} Pts`
    : category === "Estetica"
      ? ` ${details.totalPoints}% da meta`
      : ` ${details.totalPoints} pontos`
  }
</DialogDescription>
```

- [ ] **Step 5: Verificar compilacao e visual**

Run: `npm run build`
Run: `npm run dev` — clicar em profissionais de cada categoria para verificar modal

- [ ] **Step 6: Commit**

```bash
git add src/components/ProfessionalModal.tsx
git commit -m "feat(ui): atualizar ProfessionalModal com breakdown V2"
```

---

### Task 14: Atualizar EvolutionChartContainer

**Files:**
- Modify: `src/components/charts/EvolutionChartContainer.tsx`

- [ ] **Step 1: Receber rules e category como props**

Adicionar props para saber o modelo de scoring ativo.

- [ ] **Step 2: Implementar acumulacao revenue-points**

Para categorias com `scoringModel === 'revenue-points'` (Estetica/Maquiagem V2):

Em vez de acumular `service.points` por dia, o algoritmo deve:
1. Agrupar servicos por profissional e dia
2. Para cada dia, calcular `floor(faturamentoAcumuladoAteDia / 100)`
3. Somar pontos de estrelas acumulados
4. O eixo Y continua sendo "Pontos Acumulados"

Para categorias com modelo `points` (Cabelo, Unhas, Maquiagem V1):
- Manter logica atual sem mudanca

Para `revenue-percentage` (Estetica V1):
- Manter logica atual sem mudanca

- [ ] **Step 3: Verificar que tooltips nao expoe valores monetarios**

Para categorias revenue-points (Estetica/Maquiagem V2), verificar que os tooltips do grafico mostram "Pontos" e nao valores em R$. Se o chart usa `showEnhancedTooltips`, garantir que o formatter exibe pontos, nao revenue.

- [ ] **Step 4: Verificar compilacao e visual**

Run: `npm run build`
Run: `npm run dev` — verificar graficos de todas as categorias

- [ ] **Step 5: Commit**

```bash
git add src/components/charts/EvolutionChartContainer.tsx
git commit -m "feat(ui): implementar acumulacao revenue-points no grafico de evolucao"
```

---

## Chunk 5: Integration & Verification

### Task 15: Verificacao final integrada

- [ ] **Step 1: Build de producao**

Run: `npm run build`
Expected: Build completo sem erros ou warnings

- [ ] **Step 2: Verificacao visual — Regras V2 (marco/2026)**

Run: `npm run dev`

Verificar no browser com filtro de data em marco/2026:
- [ ] RegrasDoDesafio mostra regras V2 (tratamento 3pts, SPA 3pts, estrelas somam em todas)
- [ ] RegrasDoDesafio mostra "Meta excelencia profissional: Estrelas Google: XX estrelas" para todas as categorias
- [ ] RegrasDoDesafio mostra premio "SPA Especial + Voucher surpresa"
- [ ] PremiacaoPanel mostra metas V2 (50 cli + 40 trat para Cabelo, 70 cli + 10 SPA para Unhas)
- [ ] PremiacaoPanel mostra barras de progresso duplas para Cabelo e Unhas
- [ ] Rankings de Cabelo mostram badges com tratamentos validos e alerta de invalidos
- [ ] Rankings de Estetica/Maquiagem mostram "XX Pts" em vez de "%" ou "serv"
- [ ] Modal de Cabelo mostra breakdown validos vs nao contabilizados
- [ ] Modal de Estetica/Maquiagem mostra breakdown pontos faturamento + pontos estrelas
- [ ] Graficos de evolucao funcionam corretamente para todas as categorias

- [ ] **Step 3: Verificacao visual — Regras V1 (fevereiro/2026)**

Trocar filtro de data para fevereiro/2026:
- [ ] RegrasDoDesafio mostra regras V1 (tratamento 2pts, SPA 2pts)
- [ ] RegrasDoDesafio NAO mostra "Meta excelencia profissional"
- [ ] PremiacaoPanel mostra metas V1 (60 clientes Cabelo, R$200 premio)
- [ ] Rankings mantém formato V1 (% para Estetica, serv para Maquiagem)
- [ ] Sem badges de alerta de fabricante no Cabelo V1

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "feat: implementar regras V2 marco/2026 — scoring, metas, premios e constraints de fabricante"
```
