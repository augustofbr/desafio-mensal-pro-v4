# Regras V4 — Metas de Agosto/2026 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar as metas de agosto/2026 (V4) tornando a deteccao do servico especial configuravel por regra, levando Estetica para o modelo de pontos e permitindo desativar uma categoria por versao de regra.

**Architecture:** Um modulo novo de scoring puro (`src/lib/scoring.ts`) concentra a deteccao de servico especial e o calculo do ranking por pontos, hoje duplicados entre os hooks de categoria. Os hooks passam a delegar a esse modulo. A configuracao vem de dois campos opcionais em `CategoryRules` (`specialServiceMatch` e `enabled`), com fallback para o comportamento legado, de modo que V1/V2/V3 continuem produzindo resultados identicos.

**Tech Stack:** React 18, TypeScript, Vite (SWC), TanStack Query, Supabase, Tailwind, shadcn/ui. Testes com Vitest (adicionado na Task 1).

## Global Constraints

- **Vigencia da V4:** `2026-08` (formato `AAAA-MM`, comparacao lexicografica em `getRulesForDateFromVersions`).
- **Retrocompatibilidade obrigatoria:** V1, V2 e V3 nao podem mudar de resultado. Toda logica nova entra atras de campo opcional com fallback legado.
- **Comparacao de texto de servico:** sempre com `.trim()` nos dois lados e case-insensitive.
- **Deduplicacao de cliente/dia:** chave `clientName.trim() + "-" + service_date`. Nomes nulos ou vazios sao ignorados.
- **Nomes exatos de servico no Trinks:** `Cronograma Capilar [pacote]` (Cabelo), `SPA dos Pés` (Unhas), prefixo `Limpeza de Pele` (Estetica). O acento em `Pés` e obrigatorio.
- **Chaves de categoria:** o codigo usa `cabelo | unhas | estetica | maquiagem` (minusculo, sem acento) nas regras, e `Cabelo | Unhas | Estetica | Maquiagem` em `profissionais_ativos.categoria`.
- **Projeto Supabase:** ref `kxgrprxyqeuffhczaznl`. DDL via `apply_migration`, DML via `execute_sql`.
- **Nao modificar:** `src/components/ui/*` e `src/integrations/supabase/types.ts`.
- **Commits:** um por tarefa, mensagem em portugues seguindo o padrao do repo (`feat:`, `refactor:`, `fix:`, `test:`, `chore:`).

---

## File Structure

**Criar:**
- `src/lib/scoring.ts` — deteccao de servico especial + motor de ranking por pontos (logica pura, sem React)
- `src/lib/scoring.test.ts` — testes unitarios do modulo acima
- `supabase/migrations/20260801_regras_v4_agosto2026.sql` — insere a V4

**Modificar:**
- `src/lib/rulesConfig.ts` — tipos `SpecialServiceMatch`/`CategoryKey`, campos novos, `RULES_V4`
- `src/lib/categoryDisplayNames.ts` — `isCategoryActive(rules, category)`
- `src/hooks/useHairTreatmentData.ts` — delega ao motor
- `src/hooks/useManicurePedicureData.ts` — delega ao motor
- `src/hooks/useEsteticaData.ts` — caminho `points`
- `src/hooks/useProfessionalDetails.ts` — deteccao configuravel + Estetica em pontos
- `src/components/dashboard/DataRankings.tsx` — categorias ativas pela regra
- `src/components/PremiacaoPanel.tsx` — Estetica por pontos, categorias ativas, labels dinamicos
- `src/components/ProfessionalRanking.tsx` — labels dinamicos
- `src/components/ProfessionalModal.tsx` — labels dinamicos
- `src/components/admin/RulesVersionForm.tsx` — campos de match e switch de ativacao
- `package.json`, `vite.config.ts` — Vitest

---

### Task 1: Infraestrutura de testes (Vitest)

**Files:**
- Modify: `package.json`
- Modify: `vite.config.ts`
- Test: `src/lib/scoring.test.ts` (criado vazio-smoke aqui, preenchido na Task 3)

**Interfaces:**
- Consumes: nada
- Produces: comando `npm test` (roda uma vez, sem watch) e `npm run test:watch`

- [ ] **Step 1: Instalar o Vitest**

```bash
npm install -D vitest@^2.1.8
```

- [ ] **Step 2: Configurar o Vitest no Vite**

Em `vite.config.ts`, adicione o bloco `test` dentro do objeto de configuracao, logo apos `resolve`. O arquivo inteiro fica assim:

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import path from "path";

// https://vitejs.dev/config/
export default defineConfig(({ mode }) => ({
  server: {
    host: "::",
    port: 8080,
  },
  plugins: [
    react(),
  ].filter(Boolean),
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    environment: "node",
    include: ["src/**/*.test.ts"],
  },
}));
```

O ambiente e `node` porque só testamos logica pura — nao ha componentes React nos testes deste plano, logo nao precisamos de jsdom.

Se o TypeScript reclamar da propriedade `test` em `defineConfig`, troque a primeira linha por `import { defineConfig } from "vitest/config";` — essa variante aceita o bloco `test` e repassa o resto para o Vite.

- [ ] **Step 3: Adicionar os scripts de teste**

Em `package.json`, dentro de `"scripts"`, adicione as duas linhas apos `"lint"`:

```json
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview"
```

- [ ] **Step 4: Criar um teste smoke para provar que a infra roda**

Crie `src/lib/scoring.test.ts`:

```ts
import { describe, it, expect } from "vitest";

describe("infra de testes", () => {
  it("executa", () => {
    expect(true).toBe(true);
  });
});
```

- [ ] **Step 5: Rodar os testes**

Run: `npm test`
Expected: PASS — 1 arquivo, 1 teste.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json vite.config.ts src/lib/scoring.test.ts
git commit -m "chore(test): adicionar Vitest para testar o motor de scoring"
```

---

### Task 2: Tipos de configuracao em `rulesConfig.ts`

**Files:**
- Modify: `src/lib/rulesConfig.ts:1-30`

**Interfaces:**
- Consumes: nada
- Produces:
  - `export type CategoryKey = 'cabelo' | 'unhas' | 'estetica' | 'maquiagem'`
  - `export type SpecialServiceMatchType = 'exact' | 'prefix' | 'category'`
  - `export interface SpecialServiceMatch { type: SpecialServiceMatchType; values: string[] }`
  - `CategoryRules.specialServiceMatch?: SpecialServiceMatch`
  - `CategoryRules.enabled?: boolean`

- [ ] **Step 1: Adicionar os tipos no topo de `src/lib/rulesConfig.ts`**

Insira antes de `export interface CategoryRules`:

```ts
export type CategoryKey = 'cabelo' | 'unhas' | 'estetica' | 'maquiagem';

export type SpecialServiceMatchType = 'exact' | 'prefix' | 'category';

/**
 * Define como identificar o "servico especial" de uma categoria.
 * - exact: service_name igual a algum valor
 * - prefix: service_name comeca com algum valor
 * - category: category igual a algum valor
 * Comparacao sempre com trim e case-insensitive.
 */
export interface SpecialServiceMatch {
  type: SpecialServiceMatchType;
  values: string[];
}
```

- [ ] **Step 2: Adicionar os dois campos opcionais em `CategoryRules`**

Dentro da interface `CategoryRules`, apos `manufacturerConstraints: boolean;` e antes de `prize: string;`:

```ts
  manufacturerConstraints: boolean;
  /** Ausente = fallback legado por categoria (ver resolveSpecialServiceMatch) */
  specialServiceMatch?: SpecialServiceMatch;
  /** Ausente = true. Quando false, a categoria fica fora do desafio no periodo. */
  enabled?: boolean;
  prize: string;
```

- [ ] **Step 3: Verificar que o projeto ainda compila**

Run: `npm run build`
Expected: build conclui sem erros de TypeScript. Como os campos sao opcionais, nenhuma versao de regra existente precisa mudar.

- [ ] **Step 4: Commit**

```bash
git add src/lib/rulesConfig.ts
git commit -m "feat(regras): adicionar tipos de deteccao configuravel e flag de categoria ativa"
```

---

### Task 3: Deteccao de servico especial (`matchesSpecialService`)

**Files:**
- Create: `src/lib/scoring.ts`
- Test: `src/lib/scoring.test.ts` (substitui o smoke da Task 1)

**Interfaces:**
- Consumes: `CategoryRules`, `CategoryKey`, `SpecialServiceMatch` de `@/lib/rulesConfig`
- Produces:
  - `export interface ServiceRecord { professional?: string | null; service_name?: string | null; category?: string | null; client_name?: string | null; service_date?: string | null; value?: number | string | null }`
  - `export function resolveSpecialServiceMatch(rules: CategoryRules, categoryKey: CategoryKey): SpecialServiceMatch | null`
  - `export function matchesSpecialService(service: ServiceRecord, rules: CategoryRules, categoryKey: CategoryKey): boolean`

- [ ] **Step 1: Escrever os testes que devem falhar**

Substitua todo o conteudo de `src/lib/scoring.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { matchesSpecialService, resolveSpecialServiceMatch } from "./scoring";
import type { CategoryRules } from "./rulesConfig";

function makeRules(overrides: Partial<CategoryRules> = {}): CategoryRules {
  return {
    scoringModel: "points",
    clientPointValue: 1,
    specialServicePointValue: 2,
    specialServiceLabel: "Especial",
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: {},
    symbolicGoals: {},
    manufacturerConstraints: false,
    prize: "A definir",
    ...overrides,
  };
}

describe("resolveSpecialServiceMatch", () => {
  it("usa o match configurado quando existe", () => {
    const rules = makeRules({
      specialServiceMatch: { type: "exact", values: ["Cronograma Capilar [pacote]"] },
    });
    expect(resolveSpecialServiceMatch(rules, "cabelo")).toEqual({
      type: "exact",
      values: ["Cronograma Capilar [pacote]"],
    });
  });

  it("cai no fallback legado de Cabelo quando nao ha match configurado", () => {
    expect(resolveSpecialServiceMatch(makeRules(), "cabelo")).toEqual({
      type: "category",
      values: ["Tratamentos para Cabelo"],
    });
  });

  it("cai no fallback legado de Unhas quando nao ha match configurado", () => {
    expect(resolveSpecialServiceMatch(makeRules(), "unhas")).toEqual({
      type: "exact",
      values: ["SPA dos Pés"],
    });
  });

  it("retorna null para Estetica e Maquiagem sem match configurado", () => {
    expect(resolveSpecialServiceMatch(makeRules(), "estetica")).toBeNull();
    expect(resolveSpecialServiceMatch(makeRules(), "maquiagem")).toBeNull();
  });
});

describe("matchesSpecialService", () => {
  const exactRules = makeRules({
    specialServiceMatch: { type: "exact", values: ["Cronograma Capilar [pacote]"] },
  });

  it("casa por nome exato", () => {
    expect(
      matchesSpecialService({ service_name: "Cronograma Capilar [pacote]" }, exactRules, "cabelo")
    ).toBe(true);
  });

  it("ignora espacos e caixa no nome exato", () => {
    expect(
      matchesSpecialService({ service_name: "  cronograma capilar [PACOTE]  " }, exactRules, "cabelo")
    ).toBe(true);
  });

  it("nao casa nome exato diferente", () => {
    expect(
      matchesSpecialService({ service_name: "Wella Luxe Ultimate" }, exactRules, "cabelo")
    ).toBe(false);
  });

  it("nao casa por prefixo quando o tipo e exact", () => {
    expect(
      matchesSpecialService({ service_name: "Cronograma Capilar [pacote] Premium" }, exactRules, "cabelo")
    ).toBe(false);
  });

  const prefixRules = makeRules({
    specialServiceMatch: { type: "prefix", values: ["Limpeza de Pele"] },
  });

  it("casa as duas variacoes de limpeza de pele por prefixo", () => {
    expect(
      matchesSpecialService({ service_name: "Limpeza de Pele Profunda" }, prefixRules, "estetica")
    ).toBe(true);
    expect(
      matchesSpecialService({ service_name: "Limpeza de Pele simples" }, prefixRules, "estetica")
    ).toBe(true);
  });

  it("nao casa outro servico de estetica", () => {
    expect(
      matchesSpecialService({ service_name: "Cilios Fox Eyes" }, prefixRules, "estetica")
    ).toBe(false);
  });

  const categoryRules = makeRules({
    specialServiceMatch: { type: "category", values: ["Tratamentos para Cabelo"] },
  });

  it("casa pelo campo category quando o tipo e category", () => {
    expect(
      matchesSpecialService(
        { service_name: "Wella Luxe Ultimate", category: "Tratamentos para Cabelo" },
        categoryRules,
        "cabelo"
      )
    ).toBe(true);
  });

  it("nao casa quando a category e outra", () => {
    expect(
      matchesSpecialService(
        { service_name: "Wella Luxe Ultimate", category: "Cabelos" },
        categoryRules,
        "cabelo"
      )
    ).toBe(false);
  });

  it("retorna false para servico sem nome nem categoria", () => {
    expect(matchesSpecialService({}, exactRules, "cabelo")).toBe(false);
    expect(matchesSpecialService({ service_name: "   " }, exactRules, "cabelo")).toBe(false);
  });

  it("retorna false quando a lista de valores esta vazia", () => {
    const vazio = makeRules({ specialServiceMatch: { type: "exact", values: [] } });
    expect(matchesSpecialService({ service_name: "Qualquer" }, vazio, "estetica")).toBe(false);
  });

  it("usa o fallback legado quando nao ha match configurado", () => {
    expect(
      matchesSpecialService({ service_name: "SPA dos Pés" }, makeRules(), "unhas")
    ).toBe(true);
    expect(
      matchesSpecialService({ category: "Tratamentos para Cabelo" }, makeRules(), "cabelo")
    ).toBe(true);
    expect(
      matchesSpecialService({ service_name: "Limpeza de Pele Profunda" }, makeRules(), "estetica")
    ).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar os testes para confirmar que falham**

Run: `npm test`
Expected: FAIL — `Failed to resolve import "./scoring"` (o modulo ainda nao existe).

- [ ] **Step 3: Implementar `src/lib/scoring.ts`**

```ts
import type { CategoryKey, CategoryRules, SpecialServiceMatch } from "@/lib/rulesConfig";

/** Formato bruto de uma linha de trinks_services usada pelo scoring. */
export interface ServiceRecord {
  professional?: string | null;
  service_name?: string | null;
  category?: string | null;
  client_name?: string | null;
  service_date?: string | null;
  value?: number | string | null;
}

/**
 * Deteccao usada pelas versoes de regra anteriores a V4, que nao possuem
 * specialServiceMatch. Preserva o comportamento historico de V1/V2/V3.
 */
const LEGACY_MATCH: Partial<Record<CategoryKey, SpecialServiceMatch>> = {
  cabelo: { type: "category", values: ["Tratamentos para Cabelo"] },
  unhas: { type: "exact", values: ["SPA dos Pés"] },
};

export function resolveSpecialServiceMatch(
  rules: CategoryRules,
  categoryKey: CategoryKey
): SpecialServiceMatch | null {
  if (rules.specialServiceMatch) return rules.specialServiceMatch;
  return LEGACY_MATCH[categoryKey] ?? null;
}

export function matchesSpecialService(
  service: ServiceRecord,
  rules: CategoryRules,
  categoryKey: CategoryKey
): boolean {
  const match = resolveSpecialServiceMatch(rules, categoryKey);
  if (!match || match.values.length === 0) return false;

  const rawField = match.type === "category" ? service.category : service.service_name;
  const field = (rawField ?? "").trim().toLowerCase();
  if (!field) return false;

  return match.values.some((value) => {
    const target = value.trim().toLowerCase();
    if (!target) return false;
    return match.type === "prefix" ? field.startsWith(target) : field === target;
  });
}
```

- [ ] **Step 4: Rodar os testes para confirmar que passam**

Run: `npm test`
Expected: PASS — todos os testes de `resolveSpecialServiceMatch` e `matchesSpecialService`.

- [ ] **Step 5: Commit**

```bash
git add src/lib/scoring.ts src/lib/scoring.test.ts
git commit -m "feat(scoring): deteccao configuravel de servico especial com fallback legado"
```

---

### Task 4: Motor de ranking por pontos (`computePointsRanking`)

**Files:**
- Modify: `src/lib/scoring.ts`
- Test: `src/lib/scoring.test.ts`

**Interfaces:**
- Consumes: `matchesSpecialService`, `ServiceRecord` da Task 3
- Produces:
  - `export interface ScoredService { date: string; name: string; points: number; type: 'treatment' | 'spa' | 'special' | 'client' | 'star'; clientName?: string }`
  - `export interface PointsRankingEntry { professional: string; points: number; services: ScoredService[]; specialServices: number; invalidSpecialServices: number; uniqueClientDays: number; serviceCount: number; starCount: number; starPoints: number }`
  - `export interface PointsRankingInput { categoryServices: ServiceRecord[]; categoryProfessionals: string[]; starsByProfessional: Map<string, number>; rules: CategoryRules; categoryKey: CategoryKey; specialServiceType?: ScoredService['type']; isSpecialServiceValid?: (service: ServiceRecord) => boolean }`
  - `export function computePointsRanking(input: PointsRankingInput): PointsRankingEntry[]`

- [ ] **Step 1: Escrever os testes que devem falhar**

Primeiro, altere a linha de import no **topo** de `src/lib/scoring.test.ts` para incluir a funcao nova (nao adicione um segundo `import` do mesmo modulo):

```ts
import { computePointsRanking, matchesSpecialService, resolveSpecialServiceMatch } from "./scoring";
```

Depois acrescente o bloco abaixo ao **final** do arquivo, mantendo intactos os testes e o helper `makeRules` ja existentes:

```ts
describe("computePointsRanking", () => {
  const v4Cabelo = makeRules({
    clientPointValue: 2,
    specialServicePointValue: 5,
    starPointValue: 3,
    starsCountInScore: true,
    specialServiceMatch: { type: "exact", values: ["Cronograma Capilar [pacote]"] },
  });

  it("pontua servico especial, cliente unico por dia e estrelas", () => {
    const result = computePointsRanking({
      categoryServices: [
        { professional: "Brenda", service_name: "Cronograma Capilar [pacote]", client_name: "Ana", service_date: "01/08/2026" },
        { professional: "Brenda", service_name: "Escova", client_name: "Bia", service_date: "01/08/2026" },
      ],
      categoryProfessionals: ["Brenda"],
      starsByProfessional: new Map([["Brenda", 2]]),
      rules: v4Cabelo,
      categoryKey: "cabelo",
    });

    // 5 (cronograma) + 2 clientes x 2 pts + 2 estrelas x 3 pts = 5 + 4 + 6 = 15
    expect(result).toHaveLength(1);
    expect(result[0].points).toBe(15);
    expect(result[0].specialServices).toBe(1);
    expect(result[0].uniqueClientDays).toBe(2);
    expect(result[0].starCount).toBe(2);
    expect(result[0].starPoints).toBe(6);
    expect(result[0].serviceCount).toBe(2);
  });

  it("conta o mesmo cliente no mesmo dia uma unica vez", () => {
    const result = computePointsRanking({
      categoryServices: [
        { professional: "Ana", service_name: "Corte", client_name: "Maria", service_date: "05/08/2026" },
        { professional: "Ana", service_name: "Escova", client_name: "Maria", service_date: "05/08/2026" },
        { professional: "Ana", service_name: "Corte", client_name: "Maria", service_date: "06/08/2026" },
      ],
      categoryProfessionals: ["Ana"],
      starsByProfessional: new Map(),
      rules: v4Cabelo,
      categoryKey: "cabelo",
    });

    expect(result[0].uniqueClientDays).toBe(2);
    expect(result[0].points).toBe(4);
  });

  it("ignora clientes com nome vazio ou nulo", () => {
    const result = computePointsRanking({
      categoryServices: [
        { professional: "Ana", service_name: "Corte", client_name: "   ", service_date: "05/08/2026" },
        { professional: "Ana", service_name: "Corte", client_name: null, service_date: "05/08/2026" },
      ],
      categoryProfessionals: ["Ana"],
      starsByProfessional: new Map(),
      rules: v4Cabelo,
      categoryKey: "cabelo",
    });

    expect(result[0].uniqueClientDays).toBe(0);
    expect(result[0].points).toBe(0);
  });

  it("inclui profissionais da categoria sem nenhum servico com zero pontos", () => {
    const result = computePointsRanking({
      categoryServices: [],
      categoryProfessionals: ["Ana", "Bia"],
      starsByProfessional: new Map(),
      rules: v4Cabelo,
      categoryKey: "cabelo",
    });

    expect(result).toHaveLength(2);
    expect(result.every((r) => r.points === 0)).toBe(true);
  });

  it("inclui profissional que so tem estrelas", () => {
    const result = computePointsRanking({
      categoryServices: [],
      categoryProfessionals: [],
      starsByProfessional: new Map([["Debora", 3]]),
      rules: v4Cabelo,
      categoryKey: "cabelo",
    });

    expect(result).toHaveLength(1);
    expect(result[0].professional).toBe("Debora");
    expect(result[0].points).toBe(9);
  });

  it("nao soma estrelas quando starsCountInScore e false", () => {
    const semEstrela = makeRules({ starsCountInScore: false, starPointValue: 3, clientPointValue: 1 });
    const result = computePointsRanking({
      categoryServices: [],
      categoryProfessionals: ["Ana"],
      starsByProfessional: new Map([["Ana", 4]]),
      rules: semEstrela,
      categoryKey: "estetica",
    });

    expect(result[0].starCount).toBe(4);
    expect(result[0].starPoints).toBe(0);
    expect(result[0].points).toBe(0);
  });

  it("respeita isSpecialServiceValid e contabiliza os invalidos", () => {
    const result = computePointsRanking({
      categoryServices: [
        { professional: "Ana", service_name: "Cronograma Capilar [pacote]", client_name: "X", service_date: "01/08/2026" },
        { professional: "Ana", service_name: "Cronograma Capilar [pacote]", client_name: "Y", service_date: "02/08/2026" },
      ],
      categoryProfessionals: ["Ana"],
      starsByProfessional: new Map(),
      rules: v4Cabelo,
      categoryKey: "cabelo",
      isSpecialServiceValid: (service) => service.client_name === "X",
    });

    expect(result[0].specialServices).toBe(1);
    expect(result[0].invalidSpecialServices).toBe(1);
    // 5 (1 cronograma valido) + 2 clientes x 2 = 9
    expect(result[0].points).toBe(9);
  });

  it("ordena por pontos decrescente", () => {
    const result = computePointsRanking({
      categoryServices: [
        { professional: "Ana", service_name: "Corte", client_name: "M", service_date: "01/08/2026" },
        { professional: "Bia", service_name: "Cronograma Capilar [pacote]", client_name: "N", service_date: "01/08/2026" },
      ],
      categoryProfessionals: ["Ana", "Bia"],
      starsByProfessional: new Map(),
      rules: v4Cabelo,
      categoryKey: "cabelo",
    });

    expect(result[0].professional).toBe("Bia");
    expect(result[1].professional).toBe("Ana");
  });

  it("reproduz o comportamento legado de Unhas sem match configurado", () => {
    const v3Unhas = makeRules({
      clientPointValue: 1,
      specialServicePointValue: 2,
      starPointValue: 3,
      starsCountInScore: true,
    });

    const result = computePointsRanking({
      categoryServices: [
        { professional: "Jane", service_name: "SPA dos Pés", client_name: "A", service_date: "10/04/2026" },
        { professional: "Jane", service_name: "Manicure", client_name: "B", service_date: "10/04/2026" },
      ],
      categoryProfessionals: ["Jane"],
      starsByProfessional: new Map([["Jane", 1]]),
      rules: v3Unhas,
      categoryKey: "unhas",
    });

    // 2 (SPA) + 2 clientes x 1 + 1 estrela x 3 = 7
    expect(result[0].points).toBe(7);
    expect(result[0].specialServices).toBe(1);
  });
});
```

- [ ] **Step 2: Rodar os testes para confirmar que falham**

Run: `npm test`
Expected: FAIL — `computePointsRanking is not a function` / erro de import.

- [ ] **Step 3: Implementar o motor em `src/lib/scoring.ts`**

Acrescente ao final do arquivo (mantendo o que ja existe):

```ts
export interface ScoredService {
  date: string;
  name: string;
  points: number;
  type: "treatment" | "spa" | "special" | "client" | "star";
  clientName?: string;
}

export interface PointsRankingEntry {
  professional: string;
  points: number;
  services: ScoredService[];
  specialServices: number;
  invalidSpecialServices: number;
  uniqueClientDays: number;
  serviceCount: number;
  starCount: number;
  starPoints: number;
}

export interface PointsRankingInput {
  categoryServices: ServiceRecord[];
  categoryProfessionals: string[];
  starsByProfessional: Map<string, number>;
  rules: CategoryRules;
  categoryKey: CategoryKey;
  /** Rotulo do tipo no detalhamento de servicos. Default: 'special'. */
  specialServiceType?: ScoredService["type"];
  /** Quando informado, servicos especiais reprovados nao pontuam e entram em invalidSpecialServices. */
  isSpecialServiceValid?: (service: ServiceRecord) => boolean;
}

interface Accumulator extends PointsRankingEntry {
  clientDays: Set<string>;
}

function createEntry(professional: string): Accumulator {
  return {
    professional,
    points: 0,
    services: [],
    specialServices: 0,
    invalidSpecialServices: 0,
    uniqueClientDays: 0,
    serviceCount: 0,
    starCount: 0,
    starPoints: 0,
    clientDays: new Set<string>(),
  };
}

/**
 * Calcula o ranking do modelo 'points': servico especial + cliente unico por dia + estrelas.
 * Usado por Cabelo, Unhas e (a partir da V4) Estetica.
 */
export function computePointsRanking(input: PointsRankingInput): PointsRankingEntry[] {
  const {
    categoryServices,
    categoryProfessionals,
    starsByProfessional,
    rules,
    categoryKey,
    specialServiceType = "special",
    isSpecialServiceValid,
  } = input;

  const accumulators = new Map<string, Accumulator>();

  for (const professional of categoryProfessionals) {
    accumulators.set(professional, createEntry(professional));
  }

  for (const service of categoryServices) {
    const professional = service.professional;
    if (!professional) continue;

    let entry = accumulators.get(professional);
    if (!entry) {
      entry = createEntry(professional);
      accumulators.set(professional, entry);
    }

    entry.serviceCount += 1;

    if (matchesSpecialService(service, rules, categoryKey)) {
      const isValid = isSpecialServiceValid ? isSpecialServiceValid(service) : true;

      if (isValid) {
        entry.points += rules.specialServicePointValue;
        entry.specialServices += 1;
        entry.services.push({
          date: service.service_date ?? "",
          name: service.service_name || "Servico sem nome",
          points: rules.specialServicePointValue,
          type: specialServiceType,
        });
      } else {
        entry.invalidSpecialServices += 1;
      }
    }

    const clientName = service.client_name;
    if (clientName && clientName.trim()) {
      const clientDayKey = `${clientName.trim()}-${service.service_date}`;
      if (!entry.clientDays.has(clientDayKey)) {
        entry.clientDays.add(clientDayKey);
        entry.uniqueClientDays += 1;
        entry.points += rules.clientPointValue;
        entry.services.push({
          date: service.service_date ?? "",
          name: `Cliente: ${clientName}`,
          points: rules.clientPointValue,
          type: "client",
          clientName,
        });
      }
    }
  }

  starsByProfessional.forEach((starCount, professional) => {
    let entry = accumulators.get(professional);
    if (!entry) {
      entry = createEntry(professional);
      accumulators.set(professional, entry);
    }

    const starPoints = rules.starsCountInScore ? starCount * rules.starPointValue : 0;
    entry.starCount = starCount;
    entry.starPoints = starPoints;
    entry.points += starPoints;

    if (starPoints > 0) {
      entry.services.push({
        date: "",
        name: `Estrelas Google: ${starCount} estrela${starCount > 1 ? "s" : ""} aprovada${starCount > 1 ? "s" : ""}`,
        points: starPoints,
        type: "star",
      });
    }
  });

  return Array.from(accumulators.values())
    .map(({ clientDays, ...entry }) => entry)
    .sort((a, b) => b.points - a.points);
}
```

Observacao sobre `service_date`: o motor recebe a data no formato bruto do banco (`DD/MM/YYYY`) e nao converte. A conversao para exibicao (`convertDateFormat`) e responsabilidade dos hooks, que ja a aplicam antes de mandar para a UI — ver Task 6.

- [ ] **Step 4: Rodar os testes para confirmar que passam**

Run: `npm test`
Expected: PASS — todos os testes, incluindo os da Task 3.

- [ ] **Step 5: Commit**

```bash
git add src/lib/scoring.ts src/lib/scoring.test.ts
git commit -m "feat(scoring): motor compartilhado de ranking por pontos"
```

---

### Task 5: Cadastrar a versao V4

**Files:**
- Modify: `src/lib/rulesConfig.ts:86-152`
- Create: `supabase/migrations/20260801_regras_v4_agosto2026.sql`

**Interfaces:**
- Consumes: tipos da Task 2
- Produces: `RULES_V4` exportada via `RULES_VERSIONS`; linha `valid_from = '2026-08'` em `regras_desafio`

- [ ] **Step 1: Adicionar `RULES_V4` em `src/lib/rulesConfig.ts`**

Insira logo apos a constante `RULES_V2` (e antes de `const RULES_VERSIONS`). Atencao: o fallback local do repo hoje so tem V1 e V2 — a V3 vive apenas no banco. Nao crie a V3 aqui; apenas acrescente a V4.

```ts
const RULES_V4: RulesVersion = {
  id: 'v4',
  validFrom: '2026-08',
  label: 'V4 - Agosto 2026',
  cabelo: {
    scoringModel: 'points',
    clientPointValue: 2,
    specialServicePointValue: 5,
    specialServiceLabel: 'Cronograma Capilar',
    specialServiceMatch: { type: 'exact', values: ['Cronograma Capilar [pacote]'] },
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 60, minSpecialServices: 10 },
    symbolicGoals: { stars: 10 },
    manufacturerConstraints: false,
    enabled: true,
    prize: 'A definir',
  },
  unhas: {
    scoringModel: 'points',
    clientPointValue: 2,
    specialServicePointValue: 3,
    specialServiceLabel: 'SPA dos Pés',
    specialServiceMatch: { type: 'exact', values: ['SPA dos Pés'] },
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 80, minSpecialServices: 10 },
    symbolicGoals: { stars: 10 },
    manufacturerConstraints: false,
    enabled: true,
    prize: 'A definir',
  },
  estetica: {
    scoringModel: 'points',
    clientPointValue: 2,
    specialServicePointValue: 3,
    specialServiceLabel: 'Limpeza de Pele',
    specialServiceMatch: { type: 'prefix', values: ['Limpeza de Pele'] },
    starPointValue: 3,
    starsCountInScore: true,
    qualificationGoals: { minUniqueClients: 80, minSpecialServices: 10 },
    symbolicGoals: { stars: 10 },
    manufacturerConstraints: false,
    enabled: true,
    prize: 'A definir',
  },
  maquiagem: {
    scoringModel: 'revenue-points',
    clientPointValue: 1,
    specialServicePointValue: 0,
    specialServiceLabel: 'Serviços',
    starPointValue: 2,
    starsCountInScore: true,
    revenuePointConversion: 140,
    qualificationGoals: { minRevenue: 3500 },
    symbolicGoals: { stars: 10 },
    manufacturerConstraints: false,
    enabled: false,
    prize: 'Fora do desafio em agosto',
  },
};
```

- [ ] **Step 2: Registrar a V4 na lista de versoes**

Troque a linha de `RULES_VERSIONS`:

```ts
const RULES_VERSIONS: RulesVersion[] = [RULES_V1, RULES_V2, RULES_V4];
```

- [ ] **Step 3: Conferir o schema real da tabela antes de inserir**

Use a ferramenta MCP `mcp__claude_ai_Supabase__execute_sql` com `project_id: "kxgrprxyqeuffhczaznl"`:

```sql
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_name = 'regras_desafio'
ORDER BY ordinal_position;
```

Expected: colunas `id` (uuid, com default), `valid_from` (text), `label` (text), `cabelo`/`unhas`/`estetica`/`maquiagem` (jsonb), `created_at`, `updated_at`. Se `id` nao tiver default, inclua um `gen_random_uuid()` explicito no INSERT do proximo passo.

- [ ] **Step 4: Criar o arquivo de migration**

Crie `supabase/migrations/20260801_regras_v4_agosto2026.sql`:

```sql
-- V4: metas de agosto/2026
-- Cabelo passa a pontuar apenas "Cronograma Capilar [pacote]" (5 pts).
-- Estetica migra de faturamento para pontos, com "Limpeza de Pele*" (3 pts).
-- Volume de atendimento (cliente unico/dia) vale 2 pts nas tres categorias.
-- Maquiagem fica fora do desafio (enabled = false).

INSERT INTO regras_desafio (valid_from, label, cabelo, unhas, estetica, maquiagem)
VALUES (
  '2026-08',
  'V4 - Agosto 2026',
  '{
    "scoringModel": "points",
    "clientPointValue": 2,
    "specialServicePointValue": 5,
    "specialServiceLabel": "Cronograma Capilar",
    "specialServiceMatch": { "type": "exact", "values": ["Cronograma Capilar [pacote]"] },
    "starPointValue": 3,
    "starsCountInScore": true,
    "qualificationGoals": { "minUniqueClients": 60, "minSpecialServices": 10 },
    "symbolicGoals": { "stars": 10 },
    "manufacturerConstraints": false,
    "enabled": true,
    "prize": "A definir"
  }'::jsonb,
  '{
    "scoringModel": "points",
    "clientPointValue": 2,
    "specialServicePointValue": 3,
    "specialServiceLabel": "SPA dos Pés",
    "specialServiceMatch": { "type": "exact", "values": ["SPA dos Pés"] },
    "starPointValue": 3,
    "starsCountInScore": true,
    "qualificationGoals": { "minUniqueClients": 80, "minSpecialServices": 10 },
    "symbolicGoals": { "stars": 10 },
    "manufacturerConstraints": false,
    "enabled": true,
    "prize": "A definir"
  }'::jsonb,
  '{
    "scoringModel": "points",
    "clientPointValue": 2,
    "specialServicePointValue": 3,
    "specialServiceLabel": "Limpeza de Pele",
    "specialServiceMatch": { "type": "prefix", "values": ["Limpeza de Pele"] },
    "starPointValue": 3,
    "starsCountInScore": true,
    "qualificationGoals": { "minUniqueClients": 80, "minSpecialServices": 10 },
    "symbolicGoals": { "stars": 10 },
    "manufacturerConstraints": false,
    "enabled": true,
    "prize": "A definir"
  }'::jsonb,
  '{
    "scoringModel": "revenue-points",
    "clientPointValue": 1,
    "specialServicePointValue": 0,
    "specialServiceLabel": "Serviços",
    "starPointValue": 2,
    "starsCountInScore": true,
    "revenuePointConversion": 140,
    "qualificationGoals": { "minRevenue": 3500 },
    "symbolicGoals": { "stars": 10 },
    "manufacturerConstraints": false,
    "enabled": false,
    "prize": "Fora do desafio em agosto"
  }'::jsonb
);
```

- [ ] **Step 5: Aplicar a migration**

Use `mcp__claude_ai_Supabase__apply_migration` com `project_id: "kxgrprxyqeuffhczaznl"`, `name: "regras_v4_agosto2026"` e o conteudo SQL acima.

- [ ] **Step 6: Verificar que a V4 foi gravada corretamente**

```sql
SELECT valid_from, label,
       cabelo->>'specialServiceLabel'   AS cabelo_label,
       cabelo->'specialServiceMatch'    AS cabelo_match,
       cabelo->>'clientPointValue'      AS cabelo_cliente,
       estetica->>'scoringModel'        AS estetica_modelo,
       maquiagem->>'enabled'            AS maquiagem_ativa
FROM regras_desafio
WHERE valid_from = '2026-08';
```

Expected: uma linha com `cabelo_label = Cronograma Capilar`, `cabelo_cliente = 2`, `estetica_modelo = points`, `maquiagem_ativa = false`.

- [ ] **Step 7: Commit**

```bash
git add src/lib/rulesConfig.ts supabase/migrations/20260801_regras_v4_agosto2026.sql
git commit -m "feat(regras): cadastrar V4 com as metas de agosto/2026"
```

---

### Task 6: Migrar Cabelo e Unhas para o motor compartilhado

**Files:**
- Modify: `src/hooks/useHairTreatmentData.ts` (reescrita do processamento)
- Modify: `src/hooks/useManicurePedicureData.ts` (reescrita do processamento)

**Interfaces:**
- Consumes: `computePointsRanking`, `matchesSpecialService`, `ServiceRecord` de `@/lib/scoring`
- Produces: contratos de saida inalterados —
  - `useHairTreatmentData` → `{ hairData, invalidTreatments }`, onde cada item tem `professional`, `points`, `services`, `treatmentServices`, `uniqueClientDays`, `starCount`, `starPoints`, `invalidTreatmentCount` e agora tambem `specialServices` e `serviceCount`
  - `useManicurePedicureData` → array com `professional`, `points`, `services`, `spaServices`, `uniqueClientDays`, `starCount`, `starPoints` e agora tambem `specialServices` e `serviceCount`

**Nota de comportamento:** dois bugs latentes sao corrigidos aqui. (1) Os hooks somavam `+= 1` fixo por cliente/dia em vez de `rules.clientPointValue` — inofensivo ate hoje porque V1/V2/V3 usam 1, mas quebraria a V4, que usa 2. (2) Os hooks somavam estrelas ignorando `starsCountInScore` — inofensivo porque Cabelo e Unhas sempre tiveram a flag ligada. Nenhuma das correcoes altera os numeros de V1/V2/V3.

- [ ] **Step 1: Reescrever `src/hooks/useHairTreatmentData.ts`**

```ts
import { useState, useEffect } from "react";
import { convertDateFormat } from "@/lib/utils";
import { useDateFilter } from "@/contexts/DateFilterContext";
import { filterDataByDateRange } from "@/lib/dateUtils";
import { CategoryRules } from "@/lib/rulesConfig";
import { computePointsRanking, matchesSpecialService, ServiceRecord } from "@/lib/scoring";
import { ManufacturerData } from "@/hooks/useManufacturerData";
import { ProfissionalAtivo } from "@/types/profissionaisAtivos";

export interface InvalidTreatment {
  professional: string;
  serviceName: string;
  fabricante: string;
}

export function useHairTreatmentData(
  allServicesData: any[],
  categoryProfessionals: string[],
  starsByProfessional: Map<string, number> = new Map(),
  rules: CategoryRules,
  manufacturerData: ManufacturerData | null,
  profLookup: Map<string, ProfissionalAtivo>
) {
  const [hairData, setHairData] = useState<any[]>([]);
  const [invalidTreatments, setInvalidTreatments] = useState<InvalidTreatment[]>([]);
  const { getFilteredDateRange } = useDateFilter();

  useEffect(() => {
    const dateRange = getFilteredDateRange();
    const filteredData = filterDataByDateRange(allServicesData || [], dateRange);
    const categoryServices: ServiceRecord[] = filteredData.filter((service: ServiceRecord) =>
      categoryProfessionals.includes(service.professional as string)
    );

    if (categoryServices.length === 0 && starsByProfessional.size === 0 && categoryProfessionals.length === 0) {
      setHairData([]);
      setInvalidTreatments([]);
      return;
    }

    const applyManufacturerRule = rules.manufacturerConstraints && !!manufacturerData;

    const isSpecialServiceValid = (service: ServiceRecord): boolean => {
      if (!applyManufacturerRule) return true;
      const profissionalId = profLookup.get(service.professional as string)?.profissionalId;
      if (profissionalId == null) return false;
      return manufacturerData!.isTreatmentAllowed(service.service_name || "", profissionalId);
    };

    // Lista detalhada de tratamentos reprovados pela regra de fabricante
    const currentInvalidTreatments: InvalidTreatment[] = [];
    if (applyManufacturerRule) {
      for (const service of categoryServices) {
        if (!matchesSpecialService(service, rules, "cabelo")) continue;
        if (isSpecialServiceValid(service)) continue;
        const serviceName = service.service_name || "Unknown Service";
        currentInvalidTreatments.push({
          professional: service.professional as string,
          serviceName,
          fabricante: manufacturerData!.getTreatmentManufacturers(serviceName)[0] || "Desconhecido",
        });
      }
    }

    const ranking = computePointsRanking({
      categoryServices,
      categoryProfessionals,
      starsByProfessional,
      rules,
      categoryKey: "cabelo",
      specialServiceType: "treatment",
      isSpecialServiceValid,
    });

    setHairData(
      ranking.map((entry) => ({
        ...entry,
        services: entry.services.map((service) => ({
          ...service,
          date: service.date ? convertDateFormat(service.date) : "",
        })),
        treatmentServices: entry.specialServices,
        invalidTreatmentCount: entry.invalidSpecialServices,
      }))
    );
    setInvalidTreatments(currentInvalidTreatments);
  }, [
    allServicesData,
    getFilteredDateRange,
    categoryProfessionals,
    starsByProfessional,
    rules,
    manufacturerData,
    profLookup,
  ]);

  return { hairData, invalidTreatments };
}
```

- [ ] **Step 2: Reescrever `src/hooks/useManicurePedicureData.ts`**

```ts
import { useState, useEffect } from "react";
import { convertDateFormat } from "@/lib/utils";
import { useDateFilter } from "@/contexts/DateFilterContext";
import { filterDataByDateRange } from "@/lib/dateUtils";
import { CategoryRules } from "@/lib/rulesConfig";
import { computePointsRanking, ServiceRecord } from "@/lib/scoring";

export function useManicurePedicureData(
  allServicesData: any[],
  categoryProfessionals: string[],
  starsByProfessional: Map<string, number> = new Map(),
  rules: CategoryRules
) {
  const [manicureData, setManicureData] = useState<any[]>([]);
  const { getFilteredDateRange } = useDateFilter();

  useEffect(() => {
    const dateRange = getFilteredDateRange();
    const filteredData = filterDataByDateRange(allServicesData || [], dateRange);
    const categoryServices: ServiceRecord[] = filteredData.filter((service: ServiceRecord) =>
      categoryProfessionals.includes(service.professional as string)
    );

    if (categoryServices.length === 0 && starsByProfessional.size === 0 && categoryProfessionals.length === 0) {
      setManicureData([]);
      return;
    }

    const ranking = computePointsRanking({
      categoryServices,
      categoryProfessionals,
      starsByProfessional,
      rules,
      categoryKey: "unhas",
      specialServiceType: "spa",
    });

    setManicureData(
      ranking.map((entry) => ({
        ...entry,
        services: entry.services.map((service) => ({
          ...service,
          date: service.date ? convertDateFormat(service.date) : "",
        })),
        spaServices: entry.specialServices,
      }))
    );
  }, [allServicesData, getFilteredDateRange, categoryProfessionals, starsByProfessional, rules]);

  return manicureData;
}
```

- [ ] **Step 3: Verificar que os testes e o build seguem verdes**

Run: `npm test && npm run build`
Expected: PASS nos testes e build sem erro de tipo.

- [ ] **Step 4: Validar a regressao de abril na tela**

Run: `npm run dev`
Abra `http://localhost:8080`, selecione o filtro de data em **abril/2026** e confira os rankings de Cabelo e Unhas. Compare com os numeros conhecidos da base:

| Categoria | Profissional | Clientes/dia esperados |
|-----------|--------------|------------------------|
| Cabelo | Ricardo | 70 |
| Cabelo | Brenda | 62 |
| Unhas | Jane | 92 (2 SPAs) |
| Unhas | Elda | 88 (3 SPAs) |

Expected: os contadores de clientes e de servicos especiais batem com a tabela. Os pontos totais dependem tambem das estrelas aprovadas no periodo.

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useHairTreatmentData.ts src/hooks/useManicurePedicureData.ts
git commit -m "refactor(scoring): Cabelo e Unhas usam o motor compartilhado de pontos"
```

---

### Task 7: Estetica no modelo de pontos

**Files:**
- Modify: `src/hooks/useEsteticaData.ts`

**Interfaces:**
- Consumes: `computePointsRanking` de `@/lib/scoring`
- Produces: itens com `professional`, `points`, `services`, `serviceCount`, `starCount`, `starPoints`, `specialServices`, `uniqueClientDays`, `totalRevenue`, `revenuePercentage`, `revenuePoints` — os tres ultimos zerados no modelo `points`, para que a UI existente nao quebre

- [ ] **Step 1: Adicionar o caminho `points` em `src/hooks/useEsteticaData.ts`**

Mantenha os caminhos `revenue-percentage` e `revenue-points` exatamente como estao — eles atendem marco a julho. Adicione o desvio no inicio do `useEffect` e a funcao nova.

No topo do arquivo, acrescente o import:

```ts
import { computePointsRanking, ServiceRecord } from "@/lib/scoring";
```

Adicione esta funcao dentro do hook, ao lado de `processEsteticaData`:

```ts
  const processPointsData = (data: ServiceRecord[], rules: CategoryRules) => {
    const ranking = computePointsRanking({
      categoryServices: data,
      categoryProfessionals,
      starsByProfessional,
      rules,
      categoryKey: "estetica",
      specialServiceType: "special",
    });

    setEsteticaData(
      ranking.map((entry) => ({
        ...entry,
        services: entry.services.map((service) => ({
          ...service,
          date: service.date ? convertDateFormat(service.date) : "",
        })),
        // Campos de faturamento zerados: a UI legada ainda os le em alguns pontos
        totalRevenue: 0,
        revenuePercentage: 0,
        revenuePoints: 0,
      }))
    );
  };
```

E, no `useEffect`, desvie antes de chamar `processEsteticaData`. O `useEffect` completo fica:

```ts
  useEffect(() => {
    const dateRange = getFilteredDateRange();
    const filteredData = filterDataByDateRange(allServicesData || [], dateRange);
    const categoryServices = filteredData.filter(
      (service: any) => categoryProfessionals.includes(service.professional)
    );

    if (rules.scoringModel === 'points') {
      if (categoryServices.length === 0 && starsByProfessional.size === 0 && categoryProfessionals.length === 0) {
        setEsteticaData([]);
        return;
      }
      processPointsData(categoryServices, rules);
      return;
    }

    if (allServicesData && allServicesData.length > 0 && categoryProfessionals.length > 0) {
      console.log("Estética services found:", categoryServices.length, "from", categoryProfessionals.length, "professionals");
      processEsteticaData(categoryServices, rules);
    } else if (starsByProfessional.size > 0) {
      processEsteticaData([], rules);
    } else {
      setEsteticaData([]);
    }
  }, [allServicesData, getFilteredDateRange, categoryProfessionals, starsByProfessional, rules]);
```

- [ ] **Step 2: Verificar build e testes**

Run: `npm test && npm run build`
Expected: PASS e build limpo.

- [ ] **Step 3: Validar nos dois modelos**

Run: `npm run dev`
- Filtro em **abril/2026**: Estetica continua exibindo percentual da meta de faturamento (modelo `revenue-points` da V3).
- Filtro em **agosto/2026**: Estetica exibe pontos. Com a base atual (sem dados de agosto) a lista deve aparecer com os profissionais da categoria zerados, e nao vazia nem quebrada.

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useEsteticaData.ts
git commit -m "feat(estetica): suportar modelo de pontos mantendo faturamento no historico"
```

---

### Task 8: Detalhes do profissional (`useProfessionalDetails`)

**Files:**
- Modify: `src/hooks/useProfessionalDetails.ts:56-62` (filtro por categoria)
- Modify: `src/hooks/useProfessionalDetails.ts:75-156` (bloco Cabelo)
- Modify: `src/hooks/useProfessionalDetails.ts:157-219` (bloco Unhas)
- Modify: `src/hooks/useProfessionalDetails.ts:220-268` (bloco Estetica)

**Interfaces:**
- Consumes: `matchesSpecialService` de `@/lib/scoring`
- Produces: objeto `details` com o mesmo formato de hoje. Para Estetica em `points`, `summary` passa a conter `esteticaSpecialCount`, `esteticaSpecialPoints`, `esteticaUniqueClients`, `esteticaClientPoints`, `starCount`, `starPoints`

**Contexto:** este hook reimplementa o scoring para o modal de detalhes. O `CLAUDE.md` registra que qualquer mudanca de regra precisa ser feita aqui tambem, sob pena de o modal divergir do ranking.

- [ ] **Step 1: Trocar o filtro de categoria de Cabelo pelo match configurado**

Substitua o bloco das linhas 56-62:

```ts
        if (category) {
          if (category === "Cabelo") {
            filteredData = filteredData.filter(service => service.category === "Tratamentos para Cabelo");
          } else if (category === "Unhas") {
            filteredData = filteredData.filter(service => service.category === "Manicure e Pedicure");
          }
        }
```

por este bloco, que **sobe** a declaracao de `categoryRules` para antes do filtro (ela hoje vive logo abaixo, na linha 64) e passa a usar o match configurado:

```ts
        const categoryRules = category ? getCategoryRules(rules, category) : null;

        if (category === "Cabelo" && categoryRules) {
          filteredData = filteredData.filter(service =>
            matchesSpecialService(service, categoryRules, "cabelo")
          );
        } else if (category === "Unhas") {
          filteredData = filteredData.filter(service => service.category === "Manicure e Pedicure");
        }
```

Em seguida **apague** a linha original, que agora ficou duplicada:

```ts
        const categoryRules = category ? getCategoryRules(rules, category) : null;
```

Ela era a primeira das tres linhas do bloco que comeca com `const categoryRules` e termina com `const profissionalId` — mantenha as outras duas (`starCategoryKey`/`starCount` e `profissionalId`) intactas.

O filtro de Unhas continua por `Manicure e Pedicure` porque ali ele delimita o universo de servicos da categoria; a deteccao do SPA acontece depois, dentro do bloco.

Adicione o import no topo do arquivo:

```ts
import { matchesSpecialService } from "@/lib/scoring";
```

- [ ] **Step 2: Usar `clientPointValue` no bloco de Cabelo**

Dentro do bloco `if (category === "Cabelo" && categoryRules)`, no laco de `allProfessionalServices`, troque os tres literais `1` por `categoryRules.clientPointValue`:

```ts
                rawServices.push({
                  date: convertDateFormat(service.service_date),
                  name: `Cliente: ${clientName}`,
                  points: categoryRules.clientPointValue,
                  type: 'client',
                  clientName
                });
                const clientServiceName = `Cliente: ${clientName}`;
                if (!serviceSummary[clientServiceName]) {
                  serviceSummary[clientServiceName] = { name: clientServiceName, count: 0, points: 0, pointsPerService: categoryRules.clientPointValue };
                }
                serviceSummary[clientServiceName].count++;
                serviceSummary[clientServiceName].points += categoryRules.clientPointValue;
                professionalData.points += categoryRules.clientPointValue;
```

- [ ] **Step 3: Trocar a deteccao do SPA e usar `clientPointValue` no bloco de Unhas**

Dentro do bloco `else if (category === "Unhas" && categoryRules)`, troque:

```ts
            const serviceName = service.service_name || '';
            if (serviceName === "SPA dos Pés") {
```

por:

```ts
            const serviceName = service.service_name || '';
            if (matchesSpecialService(service, categoryRules, "unhas")) {
```

E, no laco de `allProfessionalServices` do mesmo bloco, aplique a mesma troca de `1` por `categoryRules.clientPointValue` feita no Step 2.

Ainda no mesmo bloco, a busca do resumo do SPA usa o nome fixo. Troque:

```ts
          const spaServicesSummary = Object.values(serviceSummary).find((s: any) => s.name === "SPA dos Pés") as any;
```

por:

```ts
          const spaServicesSummary = Object.values(serviceSummary).find(
            (s: any) => !s.name.startsWith("Cliente:")
          ) as any;
```

- [ ] **Step 4: Adicionar o caminho de pontos no bloco de Estetica**

Substitua o bloco inteiro `else if (category === "Estetica" && categoryRules) { ... }` por:

```ts
        } else if (category === "Estetica" && categoryRules && categoryRules.scoringModel === 'points') {
          const professionalData = { clientDays: new Set<string>(), specialServices: 0, points: 0 };

          filteredData.forEach(service => {
            if (!matchesSpecialService(service, categoryRules, "estetica")) return;
            const serviceName = service.service_name || "Limpeza de Pele";

            rawServices.push({
              date: convertDateFormat(service.service_date),
              name: serviceName,
              points: categoryRules.specialServicePointValue,
              type: 'special'
            });
            if (!serviceSummary[serviceName]) {
              serviceSummary[serviceName] = { name: serviceName, count: 0, points: 0, pointsPerService: categoryRules.specialServicePointValue };
            }
            serviceSummary[serviceName].count++;
            serviceSummary[serviceName].points += categoryRules.specialServicePointValue;
            professionalData.points += categoryRules.specialServicePointValue;
            professionalData.specialServices++;
          });

          allProfessionalServices.forEach(service => {
            const clientName = service.client_name;
            const serviceDate = service.service_date;
            if (clientName && clientName.trim()) {
              const clientDayKey = `${clientName.trim()}-${serviceDate}`;
              if (!professionalData.clientDays.has(clientDayKey)) {
                professionalData.clientDays.add(clientDayKey);
                rawServices.push({
                  date: convertDateFormat(service.service_date),
                  name: `Cliente: ${clientName}`,
                  points: categoryRules.clientPointValue,
                  type: 'client',
                  clientName
                });
                const clientServiceName = `Cliente: ${clientName}`;
                if (!serviceSummary[clientServiceName]) {
                  serviceSummary[clientServiceName] = { name: clientServiceName, count: 0, points: 0, pointsPerService: categoryRules.clientPointValue };
                }
                serviceSummary[clientServiceName].count++;
                serviceSummary[clientServiceName].points += categoryRules.clientPointValue;
                professionalData.points += categoryRules.clientPointValue;
              }
            }
          });

          const starPoints = categoryRules.starsCountInScore ? starCount * categoryRules.starPointValue : 0;
          totalPoints = professionalData.points + starPoints;

          const specialSummary = Object.values(serviceSummary).filter((s: any) => !s.name.startsWith("Cliente:"));
          const clientServices = Object.values(serviceSummary).filter((s: any) => s.name.startsWith("Cliente:"));

          summaryData = {
            esteticaSpecialCount: professionalData.specialServices,
            esteticaSpecialPoints: specialSummary.reduce((sum: number, s: any) => sum + s.points, 0),
            esteticaUniqueClients: professionalData.clientDays.size,
            esteticaClientPoints: clientServices.reduce((sum: number, s: any) => sum + s.points, 0),
            esteticaServiceCount: filteredData.length,
            starCount,
            starPoints,
            totalPoints
          };

        } else if (category === "Estetica" && categoryRules) {
```

E mantenha, logo abaixo, todo o corpo original do bloco de Estetica (o que trata `revenue-points` e `revenue-percentage`) sem alteracao. Ou seja: um novo `else if` para `points` entra **antes** do bloco existente, que passa a ser o caminho de faturamento.

- [ ] **Step 5: Verificar build**

Run: `npm run build`
Expected: build sem erros de TypeScript.

- [ ] **Step 6: Validar o modal nos dois periodos**

Run: `npm run dev`
- Em **abril/2026**, clique em Ricardo (Cabelo) e Jane (Unhas): o total de pontos do modal deve ser igual ao exibido no card do ranking.
- Em **agosto/2026**, clique em uma esteticista: o modal deve abrir sem erro, mostrando os blocos de pontos (nao os de faturamento).

- [ ] **Step 7: Commit**

```bash
git add src/hooks/useProfessionalDetails.ts
git commit -m "feat(detalhes): deteccao configuravel e modelo de pontos para Estetica"
```

---

### Task 9: Desativar categoria por versao de regra

**Files:**
- Modify: `src/lib/categoryDisplayNames.ts:54-59`
- Modify: `src/components/dashboard/DataRankings.tsx:66-71`
- Modify: `src/components/PremiacaoPanel.tsx:353-400`
- Modify: `src/components/dashboard/DashboardCharts.tsx:7,24-27,107,120,133,147` *(emenda 2026-07-25 — ver Step 4b)*
- Verify only (nao modificar): `src/components/charts/ComparisonChart.tsx`

**Interfaces:**
- Consumes: `RulesVersion`, `getCategoryRules` de `@/lib/rulesConfig`
- Produces: `export function isCategoryActive(rules: RulesVersion | null | undefined, category: string): boolean`

- [ ] **Step 1: Adicionar `isCategoryActive` em `src/lib/categoryDisplayNames.ts`**

Acrescente ao final do arquivo:

```ts
import type { RulesVersion } from "@/lib/rulesConfig";
import { getCategoryRules } from "@/lib/rulesConfig";

/**
 * Uma categoria participa do desafio quando esta habilitada no dashboard
 * E a versao de regra vigente nao a desativou (campo enabled).
 */
export function isCategoryActive(
  rules: RulesVersion | null | undefined,
  category: string
): boolean {
  if (!isCategoryEnabled(category)) return false;
  if (!rules) return true;
  const categoryRules = getCategoryRules(rules, category);
  return categoryRules?.enabled !== false;
}
```

O import vai no topo do arquivo, nao ao lado da funcao.

- [ ] **Step 2: Usar `isCategoryActive` em `DataRankings`**

Em `src/components/dashboard/DataRankings.tsx`, troque o import:

```ts
import { getCategoryDisplayName, PROF_CATEGORIES, isCategoryActive } from "@/lib/categoryDisplayNames";
```

E o array de categorias:

```ts
  const categories = [
    { key: PROF_CATEGORIES.CABELO, data: hairData, enabled: isCategoryActive(rules, PROF_CATEGORIES.CABELO) },
    { key: PROF_CATEGORIES.UNHAS, data: manicureData, enabled: isCategoryActive(rules, PROF_CATEGORIES.UNHAS) },
    { key: PROF_CATEGORIES.MAQUIAGEM, data: maquiagemData, enabled: isCategoryActive(rules, PROF_CATEGORIES.MAQUIAGEM) },
    { key: PROF_CATEGORIES.ESTETICA, data: esteticaData, enabled: isCategoryActive(rules, PROF_CATEGORIES.ESTETICA) },
  ];
```

O componente ja recebe `rules` via props (`DataRankingsProps`), entao nao ha mudanca de assinatura.

- [ ] **Step 3: Nao alterar `ComparisonChart` (componente morto)**

`src/components/charts/ComparisonChart.tsx` usa `isCategoryEnabled`, mas **nenhum arquivo do projeto o importa** — confirme antes de decidir:

Run: `grep -rn "ComparisonChart" src/`
Expected: apenas duas linhas, ambas dentro do proprio `ComparisonChart.tsx` (a interface de props e a declaracao do componente). Nenhum import externo.

Como o componente nao esta montado em lugar nenhum, adaptar sua deteccao de categorias exigiria adicionar a prop `rules` a um componente que ninguem renderiza. Deixe-o como esta. Se o resultado do grep mostrar algum import externo, pare: a premissa mudou e este passo precisa ser reescrito para passar `rules` a partir de quem o renderiza.

- [ ] **Step 4: Ocultar categorias inativas no `PremiacaoPanel`**

Em `src/components/PremiacaoPanel.tsx`, importe a funcao:

```ts
import { isCategoryActive } from "@/lib/categoryDisplayNames";
```

Depois, envolva cada uma das quatro chamadas de `renderCategoryAward` em uma condicional. As chaves de categoria usadas aqui sao minusculas, mas `isCategoryActive` espera a chave de `profissionais_ativos` (capitalizada), entao converta:

```tsx
          {isCategoryActive(rules, PROF_CATEGORIES.CABELO) && renderCategoryAward(
            "cabelo",
            hairWinner,
            { /* colorScheme inalterado */ },
            0
          )}
```

Repita para `unhas` (`PROF_CATEGORIES.UNHAS`), `estetica` (`PROF_CATEGORIES.ESTETICA`) e `maquiagem` (`PROF_CATEGORIES.MAQUIAGEM`), preservando os objetos de `colorScheme` e os indices existentes. Adicione o import de `PROF_CATEGORIES` junto ao de `isCategoryActive`.

Ajuste tambem o calculo de `allPrizes`, que hoje soma as quatro categorias e faz o painel exibir "Premio: ..." apenas quando todas coincidem. Passe a considerar so as ativas:

```ts
  const activeCategoryKeys = ['cabelo', 'unhas', 'estetica', 'maquiagem'].filter(
    (key) => getCategoryRules(rules, key)?.enabled !== false
  );
  const allPrizes = activeCategoryKeys.map((key) => getCategoryRules(rules, key).prize);
  const allSamePrize = allPrizes.length > 0 && new Set(allPrizes).size === 1;
  const commonPrize = allSamePrize ? allPrizes[0] : null;
```

- [ ] **Step 4b: Aplicar o mesmo gate em `DashboardCharts`**

> **Emenda ao plano (2026-07-25).** O levantamento inicial de usos de
> `isCategoryEnabled` foi truncado e nao listou este arquivo. Ele **e** montado
> (`src/pages/Index.tsx:252`) e filtra categorias em 8 pontos. Sem esta edicao, a aba de
> grafico de Maquiagem continua aparecendo em agosto.

Em `src/components/dashboard/DashboardCharts.tsx`, troque `isCategoryEnabled(X)` por
`isCategoryActive(rules, X)` nas oito ocorrencias — linhas 24-27 (lista de tabs) e
107, 120, 133, 147 (blocos condicionais) — e ajuste o import da linha 7, mantendo
`getCategoryDisplayName` e `PROF_CATEGORIES`.

Confirme antes que o componente recebe `rules` como `RulesVersion` completa (nao um
`CategoryRules` de uma categoria so). Se nao receber, adicione a prop e repasse a partir
de `Index.tsx`, onde o valor vem de `useDashboardData().rules`.

- [ ] **Step 5: Verificar build**

Run: `npm run build`
Expected: build sem erros.

- [ ] **Step 6: Validar a alternancia de meses**

Run: `npm run dev`
- Filtro em **agosto/2026**: Maquiagem nao aparece nas abas de ranking, no grafico comparativo nem no painel de premiacao. Restam tres categorias.
- Filtro em **abril/2026**: Maquiagem volta a aparecer normalmente nos tres lugares.

- [ ] **Step 7: Commit**

```bash
git add src/lib/categoryDisplayNames.ts src/components/dashboard/DataRankings.tsx src/components/PremiacaoPanel.tsx
git commit -m "feat(categorias): permitir desativar categoria por versao de regra"
```

---

### Task 10: Qualificacao e rotulos dinamicos na UI

**Files:**
- Modify: `src/components/PremiacaoPanel.tsx:38-143` (`buildWinner`), `:199-229` (`getMinimumLabel`, `getStatusLabel`)
- Modify: `src/components/ProfessionalRanking.tsx:134-169`
- Modify: `src/components/ProfessionalModal.tsx:92-133`

**Interfaces:**
- Consumes: itens de ranking com `specialServices`, `uniqueClientDays` (Tasks 6 e 7)
- Produces: nenhuma API nova — apenas comportamento de exibicao

- [ ] **Step 1: Fazer `buildWinner` tratar Estetica no modelo de pontos**

Em `src/components/PremiacaoPanel.tsx`, dentro de `buildWinner`, adicione a leitura do campo novo junto das outras, logo apos `const spaServices = leader.spaServices || 0;`:

```ts
  const specialServices = leader.specialServices || 0;
```

Depois, substitua o bloco `} else if (categoryKey === "estetica") {` por:

```ts
  } else if (categoryKey === "estetica" && categoryRules.scoringModel === "points") {
    if (goals.minUniqueClients != null) {
      const pct = Math.min((uniqueClients / goals.minUniqueClients) * 100, 100);
      progressBars.push({
        label: `${uniqueClients}/${goals.minUniqueClients} clientes`,
        current: uniqueClients,
        goal: goals.minUniqueClients,
        percent: pct,
      });
      if (uniqueClients < goals.minUniqueClients) qualified = false;
    }
    if (goals.minSpecialServices != null) {
      const pct = Math.min((specialServices / goals.minSpecialServices) * 100, 100);
      progressBars.push({
        label: `${specialServices}/${goals.minSpecialServices} ${categoryRules.specialServiceLabel}`,
        current: specialServices,
        goal: goals.minSpecialServices,
        percent: pct,
      });
      if (specialServices < goals.minSpecialServices) qualified = false;
    }
  } else if (categoryKey === "estetica") {
```

O bloco original de faturamento permanece intacto logo abaixo, agora como fallback dos meses anteriores.

- [ ] **Step 2: Tornar o rotulo do servico especial dinamico em Cabelo**

Ainda em `buildWinner`, no bloco de `cabelo`, troque a label fixa:

```ts
        label: `${treatmentServices}/${goals.minSpecialServices} tratamentos`,
```

por:

```ts
        label: `${treatmentServices}/${goals.minSpecialServices} ${categoryRules.specialServiceLabel}`,
```

E no bloco de `unhas`:

```ts
        label: `${spaServices}/${goals.minSpecialServices} ${categoryRules.specialServiceLabel}`,
```

- [ ] **Step 3: Ajustar `getMinimumLabel` e `getStatusLabel` para Estetica em pontos**

Em `getMinimumLabel`, troque a condicao de entrada:

```ts
    if (categoryKey === "cabelo" || categoryKey === "unhas" ||
        (categoryKey === "estetica" && categoryRules.scoringModel === "points")) {
```

Em `getStatusLabel`, troque:

```ts
    if (categoryKey === "estetica" || (categoryKey === "maquiagem" && bars.length === 1 && bars[0].goal === 100)) {
```

por:

```ts
    const esteticaPorFaturamento =
      categoryKey === "estetica" && getCategoryRules(rules, categoryKey).scoringModel !== "points";
    if (esteticaPorFaturamento || (categoryKey === "maquiagem" && bars.length === 1 && bars[0].goal === 100)) {
```

- [ ] **Step 4: Ajustar `getPointsDisplay` para Estetica em pontos**

Em `getPointsDisplay`, a condicao atual so cai no ramo de percentual quando o modelo e de faturamento — o que ja esta correto, porque `scoringModel === 'points'` nao entra no `if`. Nenhuma alteracao necessaria. Confirme lendo o metodo antes de seguir.

- [ ] **Step 5: Tornar dinamicos os badges do `ProfessionalRanking`**

Em `src/components/ProfessionalRanking.tsx`, no bloco de Cabelo, troque o rotulo fixo `tratam`:

```tsx
                        <span className="inline-flex items-center bg-slate-100 text-slate-600 rounded-full px-1.5 sm:px-2 py-0.5 font-medium">
                          <span className="font-mono-num">{item.treatmentServices}</span><span className="text-slate-400 ml-0.5">{rules.specialServiceLabel}</span>
                        </span>
```

No bloco de Unhas, troque `SPA`:

```tsx
                        <span className="inline-flex items-center bg-slate-100 text-slate-600 rounded-full px-1.5 sm:px-2 py-0.5 font-medium">
                          <span className="font-mono-num">{item.spaServices}</span><span className="text-slate-400 ml-0.5">{rules.specialServiceLabel}</span>
                        </span>
```

E no bloco de Estetica, mostre clientes e especiais quando o modelo for de pontos:

```tsx
                    {categoryKey === PROF_CATEGORIES.ESTETICA && (
                      rules.scoringModel === 'points' ? (
                        <>
                          <span className="inline-flex items-center bg-slate-100 text-slate-600 rounded-full px-1.5 sm:px-2 py-0.5 font-medium">
                            <span className="font-mono-num">{item.uniqueClientDays}</span><span className="text-slate-400 ml-0.5">cli</span>
                          </span>
                          <span className="inline-flex items-center bg-slate-100 text-slate-600 rounded-full px-1.5 sm:px-2 py-0.5 font-medium">
                            <span className="font-mono-num">{item.specialServices}</span><span className="text-slate-400 ml-0.5">{rules.specialServiceLabel}</span>
                          </span>
                        </>
                      ) : (
                        <span className="inline-flex items-center bg-slate-100 text-slate-600 rounded-full px-1.5 sm:px-2 py-0.5 font-medium">
                          <span className="font-mono-num">{item.serviceCount}</span><span className="text-slate-400 ml-0.5">serv</span>
                        </span>
                      )
                    )}
                    {categoryKey === PROF_CATEGORIES.CABELO && (
```

- [ ] **Step 6: Tornar dinamicos os textos do `ProfessionalModal` (bloco Unhas)**

Em `src/components/ProfessionalModal.tsx`, no bloco `else if (category === "Unhas")`, substitua os quatro textos fixos:

```tsx
                  <h4 className="font-semibold text-sm text-gray-700">{rules?.specialServiceLabel}</h4>
                  <div className="flex items-center space-x-2 mt-1">
                    <span className="text-2xl font-bold text-red-600">{summary.spaCount}</span>
                    <span className="text-sm text-gray-500">{rules?.specialServiceLabel}</span>
                  </div>
                  <p className="text-xs text-gray-600 mt-1">{summary.spaPoints} pontos total</p>
                  <p className="text-[11px] text-red-600/70 mt-2 bg-red-50 px-2 py-1 rounded-md leading-relaxed">Cada {rules?.specialServiceLabel} vale {rules?.specialServicePointValue} pts</p>
```

Depois corrija o texto de clientes, que hoje diz "vale 1 pt" fixo. Ha exatamente **duas** ocorrencias no arquivo, ambas identicas exceto pela cor:

**Linha 65** (bloco Cabelo, classe verde):

```tsx
                  <p className="text-[11px] text-green-600/70 mt-2 bg-green-50 px-2 py-1 rounded-md leading-relaxed">Cada cliente único por dia vale 1 pt</p>
```

vira:

```tsx
                  <p className="text-[11px] text-green-600/70 mt-2 bg-green-50 px-2 py-1 rounded-md leading-relaxed">Cada cliente único por dia vale {rules?.clientPointValue} pt{(rules?.clientPointValue ?? 1) > 1 ? 's' : ''}</p>
```

**Linha 127** (bloco Unhas, classe vermelha):

```tsx
                  <p className="text-[11px] text-red-600/70 mt-2 bg-red-50 px-2 py-1 rounded-md leading-relaxed">Cada cliente único por dia vale 1 pt</p>
```

vira:

```tsx
                  <p className="text-[11px] text-red-600/70 mt-2 bg-red-50 px-2 py-1 rounded-md leading-relaxed">Cada cliente único por dia vale {rules?.clientPointValue} pt{(rules?.clientPointValue ?? 1) > 1 ? 's' : ''}</p>
```

Confirme que nao sobrou nenhuma com `grep -n "vale 1 pt" src/components/ProfessionalModal.tsx` — deve retornar vazio.

O bloco de Cabelo (linha 44) ja usa `{rules?.specialServicePointValue}` para o tratamento e nao precisa de alteracao, mas o rotulo "Cada tratamento" fica desatualizado em agosto. Troque:

```tsx
                    Cada tratamento {rules?.manufacturerConstraints ? 'da marca autorizada ' : ''}vale {rules?.specialServicePointValue} pts
```

por:

```tsx
                    Cada {rules?.specialServiceLabel} {rules?.manufacturerConstraints ? 'da marca autorizada ' : ''}vale {rules?.specialServicePointValue} pts
```

- [ ] **Step 7: Renderizar os blocos de pontos da Estetica no modal**

> **Emenda ao plano (2026-07-25).** A revisao da Task 8 apontou um gap: o hook passou a
> emitir `esteticaSpecialCount`, `esteticaSpecialPoints`, `esteticaUniqueClients` e
> `esteticaClientPoints`, mas nenhum componente os le. Com a V4 (`scoringModel: 'points'`),
> o modal cai no layout de faturamento e renderiza `%` sem numero, alem de rotular o total
> como "% da meta". Nenhuma tarefa cobria isso. Corrigido aqui, que e onde o
> `ProfessionalModal` ja e editado.

Sao tres consertos no mesmo arquivo.

**(a)** Em `renderSummaryCards()`, dentro do bloco `else if (category === "Estetica")`, o
`if (rules?.scoringModel === 'revenue-points')` existente trata o caminho de faturamento.
Adicione **antes** dele um bloco para o modelo de pontos:

```tsx
    } else if (category === "Estetica") {
      if (rules?.scoringModel === 'points') {
        return (
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            <Card className="border-violet-200">
              <CardContent className="p-4">
                <div className="flex items-center space-x-3">
                  <div className="p-2 bg-violet-100 rounded-lg">
                    <Sparkles className="h-5 w-5 text-violet-600" />
                  </div>
                  <div className="flex-1">
                    <h4 className="font-semibold text-sm text-gray-700">{rules?.specialServiceLabel}</h4>
                    <div className="flex items-center space-x-2 mt-1">
                      <span className="text-2xl font-bold text-violet-600">{summary.esteticaSpecialCount}</span>
                      <span className="text-sm text-gray-500">serviços</span>
                    </div>
                    <p className="text-xs text-gray-600 mt-1">{summary.esteticaSpecialPoints} pontos total</p>
                    <p className="text-[11px] text-violet-600/70 mt-2 bg-violet-50 px-2 py-1 rounded-md leading-relaxed">Cada {rules?.specialServiceLabel} vale {rules?.specialServicePointValue} pts</p>
                  </div>
                </div>
              </CardContent>
            </Card>

            <Card className="border-violet-200">
              <CardContent className="p-4">
                <div className="flex items-center space-x-3">
                  <div className="p-2 bg-violet-100 rounded-lg">
                    <Heart className="h-5 w-5 text-violet-600" />
                  </div>
                  <div className="flex-1">
                    <h4 className="font-semibold text-sm text-gray-700">Clientes Únicas</h4>
                    <div className="flex items-center space-x-2 mt-1">
                      <span className="text-2xl font-bold text-violet-600">{summary.esteticaUniqueClients}</span>
                      <span className="text-sm text-gray-500">clientes</span>
                    </div>
                    <p className="text-xs text-gray-600 mt-1">{summary.esteticaClientPoints} pontos total</p>
                    <p className="text-[11px] text-violet-600/70 mt-2 bg-violet-50 px-2 py-1 rounded-md leading-relaxed">Cada cliente único por dia vale {rules?.clientPointValue} pt{(rules?.clientPointValue ?? 1) > 1 ? 's' : ''}</p>
                  </div>
                </div>
              </CardContent>
            </Card>
          </div>
        );
      }
      if (rules?.scoringModel === 'revenue-points') {
```

O restante do bloco de Estetica (o caminho `revenue-points` e o `return` final de
percentual) fica exatamente como esta.

**(b)** `shouldShowPointsColumn()` esconde a coluna de pontos para Estetica fora do modelo
`revenue-points` — o que agora esconderia justamente a coluna que passou a ter pontos. Troque:

```ts
  const shouldShowPointsColumn = () => {
    if (category === "Estetica") {
      return rules?.scoringModel === 'revenue-points';
    }
    return true;
  };
```

por:

```ts
  const shouldShowPointsColumn = () => {
    if (category === "Estetica") {
      return rules?.scoringModel === 'revenue-points' || rules?.scoringModel === 'points';
    }
    return true;
  };
```

**(c)** O cabecalho do modal rotula o total da Estetica como "% da meta", o que exibiria
"246% da meta" para 246 pontos. Troque:

```tsx
            {displayCategory}: {details.totalServices} serviços | {rules?.scoringModel === 'revenue-points'
              ? `${details.totalPoints} Pts`
              : category === "Estetica"
                ? `${details.totalPoints}% da meta`
                : `${details.totalPoints} pontos`
            }
```

por:

```tsx
            {displayCategory}: {details.totalServices} serviços | {rules?.scoringModel === 'revenue-points'
              ? `${details.totalPoints} Pts`
              : category === "Estetica" && rules?.scoringModel !== 'points'
                ? `${details.totalPoints}% da meta`
                : `${details.totalPoints} pontos`
            }
```

- [ ] **Step 7b: Ocultar a categoria inativa no card "Regras do Desafio"**

> **Emenda ao plano (2026-07-25).** A revisao da Task 9 apontou que
> `RegrasDoDesafio` monta sua lista a partir de um literal fixo com as quatro
> categorias, entao o card de regras da Maquiagem apareceria em agosto mesmo com ela
> fora do desafio. A secao 5 do plano dizia que este componente nao precisava de
> alteracao — verdade para os *textos*, que ja sao gerados da regra, mas nao para a
> *lista de categorias*.

Em `src/components/RegrasDoDesafio.tsx:138-139`, troque:

```ts
function buildRegras(rules: RulesVersion): CategoriaRegra[] {
  return (["cabelo", "unhas", "estetica", "maquiagem"] as const).map((catKey) => {
```

por:

```ts
function buildRegras(rules: RulesVersion): CategoriaRegra[] {
  return (["cabelo", "unhas", "estetica", "maquiagem"] as const)
    .filter((catKey) => getCategoryRules(rules, catKey)?.enabled !== false)
    .map((catKey) => {
```

O restante do corpo do `.map` fica inalterado. `getCategoryRules` ja e importado no
arquivo. Mesma semantica das outras tarefas: ausencia de `enabled` significa ativa, so
`false` explicito remove.

- [ ] **Step 8: Verificar build**

Run: `npm run build`
Expected: build sem erros.

- [ ] **Step 9: Validar visualmente**

Run: `npm run dev`
- **agosto/2026**: o card de Cabelo no painel de premiacao diz "Min. 60 clientes + 10 Cronograma Capilar"; o de Estetica diz "Min. 80 clientes + 10 Limpeza de Pele"; os badges do ranking de Cabelo mostram "Cronograma Capilar" em vez de "tratam".
- **abril/2026**: os textos voltam a "Tratamentos" e "SPA dos Pés", e Estetica volta a exibir percentual da meta.

- [ ] **Step 10: Commit**

```bash
git add src/components/PremiacaoPanel.tsx src/components/ProfessionalRanking.tsx src/components/ProfessionalModal.tsx src/components/RegrasDoDesafio.tsx
git commit -m "feat(ui): rotulos dinamicos e qualificacao por pontos na Estetica"
```

---

### Task 11: Painel admin — configurar deteccao e ativacao

**Files:**
- Modify: `src/components/admin/RulesVersionForm.tsx:21-34` (defaults), `:164-260` (campos do formulario)

**Interfaces:**
- Consumes: `SpecialServiceMatch`, `SpecialServiceMatchType` de `@/lib/rulesConfig`
- Produces: formulario capaz de criar/editar versoes com `specialServiceMatch` e `enabled`

- [ ] **Step 1: Incluir os campos novos nos defaults**

Em `defaultCategoryRules()`, adicione as duas propriedades antes de `prize`:

```ts
    manufacturerConstraints: false,
    specialServiceMatch: { type: "exact", values: [] },
    enabled: true,
    prize: "R$200",
```

- [ ] **Step 2: Adicionar os handlers de match**

Ao lado de `updateSymbolicGoal`, adicione:

```ts
  const updateMatchType = (cat: string, type: SpecialServiceMatchType) => {
    setCategories((prev) => ({
      ...prev,
      [cat]: {
        ...prev[cat],
        specialServiceMatch: {
          type,
          values: prev[cat].specialServiceMatch?.values ?? [],
        },
      },
    }));
  };

  const updateMatchValues = (cat: string, raw: string) => {
    const values = raw
      .split(";")
      .map((v) => v.trim())
      .filter((v) => v.length > 0);
    setCategories((prev) => ({
      ...prev,
      [cat]: {
        ...prev[cat],
        specialServiceMatch: {
          type: prev[cat].specialServiceMatch?.type ?? "exact",
          values,
        },
      },
    }));
  };
```

E atualize o import de tipos no topo:

```ts
import type { CategoryRules, RulesVersion, SpecialServiceMatchType } from "@/lib/rulesConfig";
```

- [ ] **Step 3: Adicionar os controles no formulario**

Dentro do `AccordionContent`, logo apos o bloco que contem "Label servico especial", insira:

```tsx
                    <div className="grid grid-cols-2 gap-3">
                      <div>
                        <Label className="text-xs text-gray-500">Deteccao do servico</Label>
                        <Select
                          value={cat.specialServiceMatch?.type ?? "exact"}
                          onValueChange={(v) => updateMatchType(key, v as SpecialServiceMatchType)}
                        >
                          <SelectTrigger className="font-body text-sm">
                            <SelectValue />
                          </SelectTrigger>
                          <SelectContent>
                            <SelectItem value="exact">Nome exato</SelectItem>
                            <SelectItem value="prefix">Nome comeca com</SelectItem>
                            <SelectItem value="category">Categoria do servico</SelectItem>
                          </SelectContent>
                        </Select>
                      </div>
                      <div>
                        <Label className="text-xs text-gray-500">Valores (separados por ;)</Label>
                        <Input
                          value={(cat.specialServiceMatch?.values ?? []).join("; ")}
                          onChange={(e) => updateMatchValues(key, e.target.value)}
                          placeholder="Cronograma Capilar [pacote]"
                          className="font-body text-sm"
                        />
                      </div>
                    </div>
```

E, no bloco de switches existente, adicione um terceiro:

```tsx
                      <div className="flex items-center gap-2">
                        <Switch
                          checked={cat.enabled !== false}
                          onCheckedChange={(v) => updateCategory(key, "enabled", v)}
                        />
                        <Label className="text-xs text-gray-500">Categoria ativa</Label>
                      </div>
```

- [ ] **Step 4: Verificar build**

Run: `npm run build`
Expected: build sem erros.

- [ ] **Step 5: Validar o formulario com a V4 real**

Run: `npm run dev`
Acesse o painel admin, abra a versao "V4 - Agosto 2026" em modo de edicao e confira: Cabelo aparece com deteccao "Nome exato" e valor `Cronograma Capilar [pacote]`; Estetica com "Nome comeca com" e `Limpeza de Pele`; Maquiagem com o switch "Categoria ativa" desligado. Salve sem alterar nada e verifique no banco que o JSON permaneceu igual:

```sql
SELECT cabelo->'specialServiceMatch', estetica->'specialServiceMatch', maquiagem->>'enabled'
FROM regras_desafio WHERE valid_from = '2026-08';
```

- [ ] **Step 6: Commit**

```bash
git add src/components/admin/RulesVersionForm.tsx
git commit -m "feat(admin): configurar deteccao de servico especial e ativacao de categoria"
```

---

### Task 12: Validacao final e documentacao

**Files:**
- Modify: `CLAUDE.md` (secao de regras de negocio)

**Interfaces:**
- Consumes: tudo das tarefas anteriores
- Produces: documentacao atualizada

- [ ] **Step 1: Rodar a suite completa**

Run: `npm test && npm run lint && npm run build`
Expected: testes PASS, lint sem erros novos, build limpo.

- [ ] **Step 2: Conferir a regressao de abril de ponta a ponta**

Run: `npm run dev`, filtro em **abril/2026**. Confira contra a base:

| Categoria | Profissional | Clientes/dia | Especiais |
|-----------|--------------|--------------|-----------|
| Cabelo | Ricardo | 70 | 0 |
| Cabelo | Brenda | 62 | 1 |
| Cabelo | Lucas | 55 | 0 |
| Unhas | Jane | 92 | 2 |
| Unhas | Elda | 88 | 3 |
| Estetica | Jeniffer | 81 | — (faturamento) |
| Estetica | Debora | 79 | — (faturamento) |

Expected: os numeros de clientes e especiais batem exatamente. Estetica continua exibindo percentual de faturamento, e Maquiagem continua visivel.

- [ ] **Step 3: Conferir agosto**

Filtro em **agosto/2026**. Expected: tres categorias visiveis (sem Maquiagem); Cabelo e Estetica com os rotulos novos; o componente "Regras do Desafio" exibindo "Cada cliente atendido no dia vale 2 pontos", "Cada Cronograma Capilar vale 5 pontos" e "Cada Limpeza de Pele vale 3 pontos" — este ultimo texto e gerado automaticamente e nao exigiu alteracao de codigo.

- [ ] **Step 4: Conferir o grafico de evolucao da Estetica**

`EvolutionChartContainer` ramifica em `rules?.scoringModel === 'revenue-points'`. Com a Estetica em `points`, ela passa a cair no caminho `else`, que acumula `service.points` por dia via `groupByDay`. Esse caminho depende de `services[].date` estar no formato produzido por `convertDateFormat` — o mesmo que Cabelo e Unhas ja usam, e que a Task 7 aplica na Estetica.

Com o filtro em **agosto/2026**, abra o grafico de evolucao da Estetica. Expected: renderiza sem erro. Sem dados de agosto na base, a linha aparece zerada — o que importa aqui e nao quebrar. Se aparecer `NaN` ou o grafico sumir, o formato de data da Task 7 divergiu e precisa ser corrigido antes de fechar.

- [ ] **Step 5: Atualizar o `CLAUDE.md`**

Na secao "Category Scoring Rules", acrescente ao final, antes de "#### Key Components":

```markdown
#### Versionamento de Regras (V4 — Agosto/2026)

A partir de agosto/2026, a deteccao do servico especial e **configuravel por regra**
via `CategoryRules.specialServiceMatch` (`exact` | `prefix` | `category`), resolvida
por `matchesSpecialService()` em `src/lib/scoring.ts`. Quando o campo esta ausente,
vale o fallback legado: Cabelo por `category === "Tratamentos para Cabelo"`, Unhas por
`service_name === "SPA dos Pés"`. Comparacao sempre com trim e case-insensitive.

O calculo do modelo `points` (servico especial + cliente unico/dia + estrelas) vive em
`computePointsRanking()`, compartilhado por Cabelo, Unhas e Estetica. `useProfessionalDetails`
continua reimplementando o scoring — mudancas de regra seguem exigindo alteracao nos dois lugares.

`CategoryRules.enabled = false` remove a categoria do desafio no periodo (abas, grafico
comparativo e painel de premiacao), via `isCategoryActive()` em `src/lib/categoryDisplayNames.ts`.

**V4 (ago/2026):** Cabelo 5 pts por `Cronograma Capilar [pacote]`; Unhas 3 pts por SPA dos Pes;
Estetica migra para `points` com 3 pts por `Limpeza de Pele*`; volume de atendimento vale 2 pts
nas tres; estrelas valem 3 pts e contam em todas; Maquiagem fora do desafio.
```

Na secao "Testing & Quality", troque "**No test framework**: Currently no unit/integration tests" por:

```markdown
- **Vitest**: testes unitarios do motor de scoring em `src/lib/scoring.test.ts`. Rode com `npm test`.
  Componentes React e hooks ainda nao possuem testes.
```

- [ ] **Step 6: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: registrar motor de scoring configuravel e regras V4"
```

---

## Notas de Execucao

**Ordem obrigatoria:** Tasks 1 → 2 → 3 → 4 estabelecem a fundacao e precisam vir nessa sequencia. A Task 5 depende da 2. As Tasks 6, 7 e 8 dependem da 4 e podem ser feitas em qualquer ordem entre si. As Tasks 9, 10 e 11 dependem das anteriores. A Task 12 e a ultima.

**Se a regressao de abril divergir:** pare e investigue antes de seguir. As causas mais provaveis sao (a) `starsCountInScore` sendo respeitado onde antes era ignorado — confira se a versao vigente tem a flag ligada; (b) `clientPointValue` diferente de 1 em alguma versao antiga; (c) mudanca na ordem do array `services`, que afeta a exibicao do modal mas nao o total de pontos.

**Dados de agosto:** a base so tem abril no momento da escrita deste plano. A validacao de agosto e estrutural (a tela renderiza, as regras corretas aparecem, nada quebra com listas vazias), nao numerica. A conferencia numerica de agosto so sera possivel quando a ingestao do Trinks voltar.
